# 给另一个 Codex 客户端的继续工作提示

请打开本项目根目录，先完整读取 `AGENTS.md` 和 `CODEX_HANDOFF.md`，然后运行 `pnpm test` 与 `pnpm build` 确认交接基线。不要直接重写现有架构。

确认当前进度后，完整读取 `SCREEN_RECOGNITION_PLAN.md`、`RECENT_CHANGES.md` 和 `src/data/observedRun.ts`，并优先处理用户提供的下一张游戏截图。十四张完整实战图已形成可重放轨迹：`720 → 1320 → 7170 → 16650 → 42732 → 68328 → 80280 → 278520 → 331980 → 425722`。第 9 回合已确认强效祛异药粉在没有不同种群可移除时仍会结算 `+154 数量`，随后刮骨刀生效，第 10 回合唯一异魔为 `523 × 814 = 425722`。手输版已支持“至多移除 2 组”的实际记录。当前候选为生骨药粉、孪生激素-蛊虫、混合活蛭溶液；其中生骨药粉对唯一异魔的已确认下界是 `+40 数量`，其余两张尚未结构化，不要虚构具体数值。每局共有 3 次全局洗牌；无正收益普通候选且有药剂箱时先展开药剂箱，没有药剂箱再洗牌。仍需补充生骨药粉命中骨卫兵的额外 `+127` 分支、孪生激素同名新增组规则与混合活蛭溶液在同稀有度不足两组时的表现。每个里程碑完成后更新 `README.md`、`QA.md` 和 `CODEX_HANDOFF.md`。
