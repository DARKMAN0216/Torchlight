# 后续完善与训练方案

日期：2026-09-07。以下样本量、性能和验收门槛是本项目的建议起点，需按实测调整，不是已经达到的结果。

目前最值得投入的是补全规则和验证模拟器。神经网络不负责猜测卡牌规则；待模拟可信、搜索确实成为速度瓶颈后，再训练价值模型加速。

## 第一阶段：把概率原型接成可用的实验模式

建议下一次开发完成以下闭环：

1. 在桌面增加“概率规划（实验）”，展示支持范围、显式假设、缺失参数和牌池名称。
2. 使用 Web Worker 运行 recommend；收到新F8局面时取消旧搜索，使用局面版本号避免迟到结果覆盖新推荐。
3. 先接只读推荐和目标高亮。玩家操作后的新F8必须再次校验，由真实状态覆盖预测。
4. 保存当前局面、候选、常驻获得回合、搜索种子/预算、所选动作和前后观测。增加 JSONL 导出、按局查看和人工修正功能。
5. 增加“器械顺序敏感”详情。三个情景推荐不一致时同时列出，供玩家判断。

验收：新旧推荐模式分别可用；待核对状态无新推荐；旧搜索不会覆盖新局面；一局重启后可恢复常驻获得回合和重抽次数；导出的真实记录能离线重放。完成后再按项目安全升级脚本生成安装包。

## 第二阶段：优先补规则，而不是平均地扩充所有卡牌

| 优先级 | 缺口 | 需要的数据或规则 | 完成标准 |
|---|---|---|---|
| P0 | 人蛹、刮骨刀及跨门槛顺序 | X=0/1/2/3蛊虫；数量接近275；操作前后状态 | 区分重复X次含不含首次，识别器械顺序；每个边界有回归 |
| P0 | 新怪基础属性 | 无相关常驻时的添加记录；完整名字、种群、稀有度、数量、单位活性 | 可靠生成字典，区分基础属性与卡面附加数值 |
| P0 | 稀有度变异 | 同一牌在不同已有成长数值下的转换，尽量无常驻干扰 | 比较保留成长、加差值、数值重置模型；独立样本验证 |
| P0 | 融合与复制 | 一组融合、混合稀有度、满槽、同名复制前后 | 输出稀有度/身份和事件顺序明确，不再用占位推断 |
| P1 | 药剂箱 | 同轮展开3/5张、展开池排除药箱、是否可重复、特殊倾向 | 同轮状态机和递归次数正确，不错误消耗培养回合 |
| P1 | 完整普通牌池 | 每轮所有候选，不只记录玩家选中的那张 | 按轮次/阵容条件/阶段筛选，未知牌不会从未来池中消失 |
| P1 | 特殊连续药剂 | 全链候选和实际选择，阶段转换与断链条件 | 每条链能从入口模拟到终点，重抽/跳选行为明确 |
| P1 | 额外奖励常驻 | 获得门槛、奖励阶段、候选、实际获得回合 | 分离“同轮追加器械”和“培养回合结束”，正确延迟生效 |
| P2 | 其余常驻 | 触发条件、重复、并列目标、多个事件相互影响 | 单器械、双器械及跨条件回归通过 |

先做会改变推荐结论的缺口。例如人蛹到底触发X还是X+1次，影响未来每一轮；比给冷门牌随意补一个分数更值得先确认。

每条新规则记录：来源截图/卡面、游戏版本、确认状态、适用范围、仍有争议的解释。已证实的规则不能被后续少量噪声自动覆盖。

## 第三阶段：采集与校准概率

建议先整理20～50局完整记录作为第一批审计数据，优先覆盖不同常驻和阵容。数量达标本身不代表覆盖充分。罕见机制不足时，单独设计目标测试。

每轮记录：

- runId、stepId、游戏版本、规则版本、时间和截图索引。
- 选择前六槽：slotId、monsterId、种群、稀有度、数量、单位活性。
- 轮次、全部候选、常驻列表及获得回合、重抽余量、特殊阶段。
- 玩家所选卡牌与可选目标；随机实际落点单独记录，不能混成玩家选择。
- 操作后完整状态；能观察到的中间状态与事件。
- 识别置信度、人工复核、拒绝原因、是否隔离了单个效果。

概率更新原则：

1. 发牌分布统计所有“出现”的候选。只看被选择的卡会引入选择偏差。
2. 先按确定条件划分合法池，再估计池内权重；不能把条件导致的缺席当作低概率。
3. 随机目标按当时合法目标数统计，保留并列目标和连续命中关系。
4. 75%等卡面明确概率先保留原文值，用数据检查一致性；不要因为几次失手就修改。
5. 等概率是当前可替换先验。估计权重时保留平滑与不确定区间，避免少样本的0次出现变成永久零概率。
6. 稀有度转换分别评估“绝对重置”“保留成长加差值”等候选机制；用留出的真实记录比较每槽误差和终局误差。
7. 卡牌附加数量、常驻加成和变异基础变化分别归因。多事件混合的一整轮不得直接喂入稀有度转换样本库。

验收门槛建议：已确认的确定性规则在留出回放中全部精确一致；任何失败都要归类为识别错误、规则错误或版本变化。概率效果使用区间覆盖率、均值误差、分位数误差评估，不要求每次随机结果等于期望。

## 第四阶段：先证明搜索策略有效

固定一套留出场景，至少包含早期建阵、中期常驻协同、末轮即时收益、六槽满员、剩余重抽0/1/3、稀有度变化、删除和融合连锁。

比较四个基线：

| 策略 | 用途 |
|---|---|
| 合法随机选择 | 最低能力基线 |
| 即时总活性最大化 | 检查未来搜索是否确实带来收益 |
| 现有启发式推荐 | 检查相对当前产品是否改善 |
| 概率搜索 | 目标策略；固定时间或固定转移预算比较 |

每个场景先用相同起点、多组固定种子运行配对比较，再用全新的种子复核。报告终局均值、p10、平均排名损失、推荐稳定性、拒绝率和p50/p95耗时；不能只挑几局高收益截图。

建议先用每动作128～512条轨迹评测，再逐步增加，检查推荐是否稳定。状态转移预算必须相同或明确标出，避免把花了几十倍时间的策略当作免费提升。

性能优化顺序：

1. 剖析 cloneState、目标组合、发牌和规则查找的耗时，先缓存与使用结构化索引。
2. 减少重复局面，缓存时必须包括常驻年龄、阶段、重抽和可见候选，不能只比较怪物总活性。
3. 对接近的根动作追加样本；明显劣势动作减少预算。保留真实样本数，处理自适应采样的误差。
4. 小状态精确枚举随机分支，大状态采样。机会节点按概率平均，不能取最大值。
5. 若 Beam 经常剪掉晚成型路线，再考虑改为 MCTS，保留纯规则模拟器和当前回归。

第一版不设必须优于旧策略的虚假结论。只有在留出集上得到可重复改善，且重要场景无明显退化，才建议成为默认模式。

## 第五阶段：训练价值模型，作为搜索加速器

启动条件：关键规则与普通牌池覆盖可用，真实回放通过，概率误差可接受；同时，扩大搜索确实有效但已超过交互时间预算。没有满足这些条件就继续完善模拟器。

“策略模型缩小候选、价值模型评估局面，再结合树搜索”已有成熟研究实例，但不代表该游戏可以直接复用其训练规模或性能。此处只借鉴分工方式。[Silver 等，Nature 2016](https://doi.org/10.1038/nature16961)

### 5.1 数据与标签

先从可靠真实局面出发，通过已校准模拟器生成可达状态。不要直接拼随机数制造不可能的六槽局面。第一批可尝试1万～5万个状态，按轮次、常驻、种群、稀有度与槽位占用分层采样。

为每个状态/合法动作，用高预算搜索生成教师标签：

- 动作的最终活性均值、方差和样本数。
- 状态价值、各动作相对价值差和最优动作集合。
- 卡牌与具体目标；多个相近或等价目标保留并列，避免强迫模型学习任意槽位偏好。
- 搜索预算、教师版本、牌池版本、种子和假设情景。

可从每动作512～2048条轨迹开始，针对排名接近者追加；需要先小批实测CPU时间，再决定总规模。纯合成标签必须标记来源，不能宣称为真实对局证据。

### 5.2 输入与网络

输入包括六个固定槽位的占用、种群、稀有度、monsterId、数量、单位活性；轮次、剩余重抽、阶段；常驻ID/获得年龄/启用状态；当前候选和合法动作掩码。

必须保留左右位置：邻槽删除和第一空槽生成使该游戏不具有任意槽位置换不变性。不能直接随机打乱六个槽位做增强。

先建立简单回归基线，再尝试小型神经网络。建议先只训练价值输出，预测最终活性；误差稳定后再增加策略输出，预测卡牌及目标组合的偏好。非法动作由规则引擎屏蔽，模型无权扩大合法动作集合。

可用 log1p 缩放数值输入以改善尺度；若价值标签也取对数，必须处理反变换偏差。不能把 exp(E[log收益]) 误称为 E[收益]。评价和最后选牌都回到真实总活性单位。

### 5.3 训练集划分

以整局 runId 划分训练、验证、测试（例如70/15/15）；同一局的所有轮次、增强状态和教师rollout放在同一组。再留出部分规则版本或常驻组合，检查新场景泛化。所有归一化参数只在训练集拟合。

这种按组划分可避免同一来源的高度相关样本同时进入训练和测试；组间隔离是 GroupKFold 等方法明确支持的使用场景。[scikit-learn 分组交叉验证文档](https://scikit-learn.org/stable/modules/cross_validation.html#cross-validation-iterators-for-grouped-data)

### 5.4 接入与验收

初期价值模型只用于 Beam 排序或模拟截断估值。根动作最终仍用规则模拟和独立采样复核，遇到未见ID、未知规则、顺序敏感或高不确定局面时回退到搜索/人工核对。

不要仅看训练损失。建议验收：在同一留出集上，价值模型辅助方案在相近终局收益下显著降低延迟；或相同时间预算下提高终局收益。均值、p10、重要常驻场景和拒绝率都应报告。

先离线回放，再启用仅记录不展示的影子推荐，再做可选实验模式。达到门槛后才替代默认策略。每次训练保存模型版本、数据摘要、参数、评测报告和回滚入口。

## 建议下一轮的具体交付

优先做“概率规划实验入口 + 决策日志导出 + 人蛹/刮骨刀顺序采样模板”，同时补怪物基础字典。这样现有原型能获得可信实战数据，后续扩大卡牌覆盖和训练都有依据。

当前未开始神经网络训练，也没有可宣称收敛的模型权重。现有19牌/7常驻原型和53项新增测试，是后续规则、搜索和数据工作共用的基础。
