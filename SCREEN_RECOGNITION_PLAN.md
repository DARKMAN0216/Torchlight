# 游戏截图识别方案

## 已固定参考样本

- 文件：`design/references/surgery-preparation-1920x1080.png`
- 原始分辨率：1920 × 1080；后续样本应尽量保留完整游戏画面，不裁剪、不缩放。
- 阶段：手术准备；底部三张牌是在开局选择一张手术用具，不是普通回合候选牌。
- 左侧可见：第 1/13 回合、最终活性 720、下档奖励 2000、已获得奖励 0/2。
- 六个培养槽中前四槽有怪物，后两槽为空。
- 前四组均显示组总活性 180，以及 `15 × 12`；四组之和与最终活性 720 一致。
- 怪物名称依次为红斑金鱼、巡察卫兵、禁典学者、邪眼翼兽。
- 三张手术用具依次为人蛹标本、挛缩指爪、孵化囊；截图中孵化囊处于高亮选择状态。

另有一组同分辨率连续样本：

- 生效前：`design/references/round-1-before-birth-bone-powder-1920x1080.png`；当前常驻为挛缩指爪，候选为生骨药粉、活性育卵激素、卵壳药粉。
- 生效后：`design/references/round-2-after-birth-bone-powder-1920x1080.png`；生骨药粉实际随机命中第 4 槽邪眼翼兽。
- 生效前第 4 槽为 `15 × 12 = 180`，生效后为 `15 × 52 = 780`；最终活性由 720 变为 1320，净增 `15 × 40 = 600`。
- 这组连续画面已经确认左因子是单体活性、右因子是数量，组总活性为两者乘积。
- 因命中对象不是骨卫兵，本样本只确认生骨药粉的随机一组 `+40 数量` 主效果；骨卫兵额外 `+127 数量`、移除一组非骨卫兵，以及挛缩指爪随后的随机 `+150 数量` 仍待连续截图确认。

强效祛异药粉连锁结果样本：

- 文件：`design/references/round-3-after-potent-exorcising-powder-chain-1920x1080.png`。
- 前一回合状态为蛊虫 `15 × 12`、骨卫兵 `15 × 12`、觉醒者 `15 × 12`、异魔 `15 × 52`，合计 1320。
- 强效祛异药粉选择骨卫兵后使其 `+154 数量`；画面结果表明蛊虫与异魔被移除。
- 两次挛缩指爪分别命中骨卫兵和觉醒者，因此骨卫兵为 `12 + 154 + 150 = 316`，觉醒者为 `12 + 150 = 162`。
- 最终活性严格吻合 `15 × 316 + 15 × 162 = 4740 + 2430 = 7170`，确认“每移除一组非骨卫兵就独立触发一次挛缩指爪”。
- 第 3 回合新候选为软脑膜溶液、脊髓溶液-觉醒者、靶向异种激素。

软脑膜溶液与奖励阶段样本：

- 文件：`design/references/round-3-after-soft-meningeal-reward-selection-1920x1080.png`。
- 软脑膜溶液选择数量 316 的普通骨卫兵，单体活性由 15 增至 45；组总活性为 `45 × 316 = 14220`。
- 觉醒者保持 `15 × 162 = 2430`，最终活性严格吻合 `14220 + 2430 = 16650`。
- 回合仍显示 3/13，但底部已从药剂候选切换为“选择一种手术用具”，因此识别阶段新增 `surgeryRewardSelection`；何时推进至第 4 回合仍等待选择后的下一张截图确认。
- 三张奖励手术用具为生铁骨锯、蠕动脊髓、生皮革拘束带。挛缩指爪图标仍在右上角，但新用具是追加还是替换，需要选择后截图最终确认。
- 当前三步已写入 `src/data/observedRun.ts`，作为后续战略权重和规则变更的可重放实战轨迹。

追加常驻与药剂箱展开样本：

- `round-4-writhing-spinal-added-persistent-1920x1080.png` 确认挛缩指爪与蠕动脊髓同时出现在右上角；手术用具是追加常驻，选完后回合由 3 推进至 4。
- `round-4-large-potion-box-expanded-1920x1080.png` 确认药剂箱只在当前回合展开 5 张随机药剂，不改变怪物状态、总活性或回合数。
- 五张牌从左到右为麻药酊剂-觉醒者、生骨药粉、蜕生皮溶液、脑雾酊剂、活殖药粉。
- `expandedPotionSelection1920` 已记录五个从左到右的归一化卡牌区域；后续样本可继续校准旋转卡牌的外接框。

脑雾酊剂与第二次奖励样本：

- `round-4-after-brain-fog-second-surgery-reward-1920x1080.png` 显示脑雾酊剂命中槽位 3 的觉醒者后，该组为稀有、`176 × 162 = 28512`；连同骨卫兵 14220，最终活性为 42732。
- 由前后状态可确认本次单体活性变化为 `15 + 41 + 120 = 176`；其中 `+41` 来自卡面，额外 `+120` 与觉醒为稀有同时发生。是否属于所有稀有度升阶的通用规则仍不可外推。
- 同一画面仍为 4/13，并切换回三张手术用具布局；候选为生皮革拘束带、脏污刮骨刀和簇生虫卵。

第 5 回合样本：

- `round-5-dirty-bone-scraper-added-1920x1080.png` 显示脏污刮骨刀图标已追加在右上角，回合进入 5/13，但两组怪物数值和最终活性 42732 完全不变。
- 该状态确认新获得的回合结束常驻不会在奖励选择完成时追溯触发；脏污刮骨刀的首次触发机会是第 5 回合药剂结算结束后。
- 三张药剂从左到右为消化酶溶液、灰质脊髓溶液、麻药酊剂-异魔；继续沿用三卡药剂布局。

第 6 回合样本：

- `round-6-after-digestive-enzyme-and-dirty-bone-scraper-2560x1440.png` 是首张 2560 × 1440 完整画面，可用于验证归一化区域跨分辨率换算。
- 回合为 6/13，最终活性为 68328；槽位 2 是普通骨卫兵 `126 × 316 = 39816`，槽位 3 是稀有觉醒者 `176 × 162 = 28512`，其余槽位为空。
- 相对第 5 回合，骨卫兵单体活性 `45 → 106 → 126`：先由消化酶溶液增加 61，再由脏污刮骨刀在回合结束增加 20；左侧空槽没有阻止药剂结算。
- 三张药剂从左到右为脊髓溶液-异魔、活殖药粉、益生霉溶液；画面中右侧卡牌处于高亮不代表已经结算。

第 7 回合样本：

- `round-7-after-active-cultivation-and-dirty-bone-scraper-2560x1440.png` 显示回合 7/13、最终活性 80280。
- 槽位 2 是普通骨卫兵 `146 × 316 = 46136`，槽位 3 是稀有觉醒者 `176 × 194 = 34144`；其余槽位为空，两组合计严格等于 80280。
- 相对第 6 回合，只有稀有觉醒者数量 `162 → 194`，确认活殖药粉本次只命中该组；普通骨卫兵数量保持 316。刮骨刀随后使骨卫兵单体活性 `126 → 146`。
- 三张药剂从左到右为蜕生皮溶液、细肢药粉-蛊虫、稀有药剂箱（小）。右侧药剂箱卡面说明包含 3 支随机药剂，且更有可能出现高稀有度药剂。

融合机制说明样本：

- `fusion-mechanism-description-393x149.png` 明确写明：移除多组怪物并添加一组新怪物，新怪物的活性、数量分别为被移除怪物的活性、数量总和。
- 说明列出的生效机制为“移除、添加、融合”；用户进一步明确融合确实会产生移除与添加事件，因此会触发挛缩指爪与蠕动脊髓。

第 8 回合融合连锁样本：

- `round-8-after-molting-skin-fusion-chain-2560x1392.png` 是带 Windows 标题栏的窗口截图，游戏内容区不是标准 2560 × 1440，识别前需要先裁掉窗口边框。
- 回合为 8/13，唯一怪物位于槽位 1，为普通异魔，`422 × 660 = 278520`；其余五槽为空。
- 数量 `660 = 316 + 194 + 150`，确认融合移除稀有觉醒者后挛缩指爪触发一次并命中唯一新增组。
- 单体活性 `422 = 146 + 176 + 80 + 20`，确认蠕动脊髓本次 75% 分支成功、将新增融合组变异为异魔并 `+80`，随后脏污刮骨刀再 `+20`。
- 新怪物出现在最左侧空槽，确认融合按“移除后添加新组”重新分配槽位，而不是保留首个目标原槽位。
- 第 8 回合候选从左到右为药剂箱（小）、脊髓溶液-异魔、消化酶溶液。

第 9 至第 10 回合样本：

- `round-9-after-digestive-enzyme-and-dirty-bone-scraper-2560x1392.png` 显示第 9 回合唯一异魔为 `503 × 660 = 331980`，验证消化酶溶液的 `+61` 与刮骨刀的 `+20` 均已结算。
- `round-10-after-potent-exorcising-powder-and-dirty-bone-scraper-2560x1392.png` 显示第 10 回合为 `523 × 814 = 425722`。
- `round-10-current-checkpoint-1920x1080.png` 是迁移后的同状态复核图；回合、唯一异魔数值、最终活性、三张常驻和三张候选均与上一张一致，可作为跨环境继续点。
- 数量 `660 → 814` 确认强效祛异药粉在没有其他种群可移除时仍结算 `+154` 主效果；单体活性 `503 → 523` 来自刮骨刀。该结果将“移除 2 组”校准为可用目标不足时的至多 2 组。
- 第 10 回合候选从左到右为生骨药粉、孪生激素-蛊虫、混合活蛭溶液。

卡牌原文已用项目数据表交叉核对：

- 人蛹标本：回合结束时，按当前蛊虫组数 X，随机 X 组怪物各 +8 数量，并重复生效 X 次。
- 挛缩指爪：移除非骨卫兵怪物时，使随机 1 组怪物 +150 数量。
- 孵化囊：至少有 2 组蛊虫时，每次添加怪物，使所有怪物 +45 数量。

## 已实现的识别基础

- `src/recognition/layout.ts` 使用归一化坐标定义阶段标题、回合、最终活性、六个怪物槽和三张卡牌区域，可按截图分辨率换算像素区域。
- 同一套锚定区域已增加普通药剂选择阶段 `potionSelection`，可覆盖这组生骨药粉前后样本。
- `src/recognition/contracts.ts` 可分别承载屏幕阶段、回合、总回合数、最终活性，以及每槽的名称、种群、稀有度、数量、单体活性和显示总活性。
- `src/recognition/validation.ts` 使用界面自身的算术关系交叉校验 OCR，避免只依赖单次文字识别。
- 核心 `GameState` 已使用 `unitActivity` 保存单体活性，并实时派生组总活性与最终活性；旧存档会换算迁移。

## 推荐识别流水线

1. 先按“手术准备”等固定标题判断当前屏幕阶段，选择对应布局配置。
2. 通过左侧 HUD、六个培养槽中心和底部确认按钮作为锚点，修正分辨率、画面缩放与轻微偏移。
3. 对数字使用局部高对比度预处理后 OCR；回合、最终活性和每槽三组数字分开识别。
4. 空槽先用固定培养罐区域做图像差分；非空槽再识别小型种群图标，怪物名称 OCR 只作为第二证据。
5. 手术用具优先用卡图模板和标题联合匹配；卡面正文只用于校验，不把整段 OCR 直接当规则执行。
6. 使用 `数量 × 单体活性 = 组总活性`、`各组总活性之和 = 最终活性` 做一致性检查。
7. 低置信度、算术不一致或未知卡牌必须进入人工确认，不直接写入决策状态。

## 尚不能由当前截图确认

- 稀有觉醒者的红色稀有度标识已有一例算术一致样本，但其他种群和稀有度仍不能只按名字颜色猜测。
- 3 张和 5 张候选布局均已有样本；选择过程动画及随机结果提示方式仍无样本。
- 生骨药粉命中骨卫兵后的额外 `+127` 与移除顺序仍未确认；强效祛异药粉触发挛缩指爪两次已经由最终结果确认。
- 多个同时具有回合结束效果的追加手术用具按什么顺序结算尚未确认。
- 2560 × 1440 全屏样本已确认仍为 16:9；其他宽高比、窗口模式或 UI 缩放下是否保持相同比例尚未验证。

## 下一批最有价值的截图

1. 继续当前第 10 回合：若选择生骨药粉，验证唯一异魔数量 854、刮骨刀后单体活性 543 和总活性 463722；若选择另两张，完整记录其未确认机制。
2. 用生骨药粉命中骨卫兵，分别截图选择前、药粉结算后和挛缩指爪连锁结算后，确认该卡专属额外分支。
3. 截一次稀有度提升前后，用于确认稀有度边框、名字颜色或图标变化。
4. 截取下一次融合的动画结束前后，用于判断能否自动区分移除、添加和常驻触发提示。
5. 若随机命中过程有高亮或结果提示，截动画结束前后，用于判断能否自动定位实际目标。
6. 首次通过蠕动脊髓添加怪物时，截图变异成功或失败后的结果，确认 +80 与种群变异顺序。
7. 若后续融合时蠕动脊髓未触发，保留失败结果截图，为自动识别 75% 分支补充反例。
8. 若选择稀有药剂箱（小），截图展开后的三张牌，校验同回合展开流程与实际稀有度分布。
