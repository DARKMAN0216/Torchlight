# 设计基线

视觉概念：[`design/concept.png`](design/concept.png)

## 设计系统

- 背景：石墨黑 `#0c1012`，面板 `#14191c`，细边框 `#313a3e`。
- 强调色：暖琥珀 `#dfa53e`；危险状态使用克制的暗红。
- 卡牌：暖灰纸张表面、深色文字、窄竖向结构；推荐卡使用琥珀描边。
- 字体：Windows 中文系统字体，主界面信息密度偏高但控制文字不低于 10px。
- 容器：左状态编辑、中候选工作区、右推荐检查器；不使用无关图表和营销模块。
- 响应式：宽屏三栏；中等宽度下推荐面板移至底部；窄屏改为单列。

## 组件清单

- `StatePanel`：常驻卡、轮次、模式和怪物表。
- 六槽怪物表：按位置记录种群、稀有度、数量和活性；普通白 `#ecefed`、魔法蓝 `#5aa1ef`、稀有黄 `#e4c247`、首领红 `#df5a4d`。
- `CandidateCardView`：可切换卡名、效果摘要、即时增量和警告。
- `RecommendationPanel`：推荐、结算轨迹、排名和示例数据警告。
- `RedrawStrip`：保留、重抽 3 张和重抽 5 张的当前一步期望。
- `recognition/contracts.ts`：后续屏幕识别适配器接口。
