# 自然牌局归档与经验学习：第一阶段技术验证

日期：2026-09-07。状态：独立离线原型，尚未接入桌面自动采集、尚未安装；现有推荐、F8、识别服务与个人存档不变。

## 目标与结论

玩家不需要控制发牌，不需要刻意重现某种开局。记录自然发生的整场牌局，并按常驻组合、种群、稀有度、回合、槽位占用、实际选择及可见变化归类。后续从这些经验训练模型，而不是把玩家的每次选择直接标记为最优。

本阶段验证两件事：

1. 真实记录可以归档、去重、分类、回放；不完整记录不会变成错误训练标签。
2. 从完整牌局构建数据集、学习经验价值、整局隔离评测、保存版本产物的技术流程可运行。

不宣称已经学会完整游戏、已经训练强化学习策略或提高了实战收益。

## 实现

| 文件 | 职责 |
|---|---|
| `src/learning/archive.ts` | 版本化牌局/步骤/画面协议，未知卡名保留，追加去重、终局确认、数据质量检查、JSONL |
| `src/learning/dataset.ts` | 按局归类、全候选统计、连续性检查、按runId稳定拆分、纯前态特征 |
| `src/learning/value.ts` | 可由数据更新的最近邻经验价值基线、按局等权标准化、独立留出评测 |
| `src/learning/verification.ts` | 历史轨迹导入、三段已验证回放、明确标为合成的完整牌局技术验证 |
| `scripts/run-learning.mjs` | 验证、审计、不可变归档、列出修订、离线训练命令 |
| `scripts/test-learning-cli.mjs` | 临时目录真实子进程测试，验证去重、修订保留、拒绝覆盖、数据不足阻断 |

不新增依赖，沿用TypeScript/Vite工具链。新模块不依赖React，不会调用游戏窗口、识别服务或实际用户数据目录。

## 记录语义

- 一场牌局一个`runId`；11次选择不能当成11场独立训练数据。
- `RunStep`包含操作前画面、实际选择、操作后画面及是否确认中间只有一次操作。
- 全部候选均保存名称及可选的稳定ID。未建模卡牌仍可归档，不依赖规划器先算出效果。
- `selectedSlotIds`只记录玩家指定目标；`observedRandomSlotIds`记录实际随机落点，不能互换。
- `click-observed`只是点击证据，不等于已验证选择；本原型在人工确认前不将其纳入可靠训练对。
- `offerId`标识一次实际发牌/洗牌/药箱展开；同牌面重拍沿用ID，真实重抽即使卡名相同也要新ID。当前原型不会仅根据内容自动推断此ID。
- 前后空槽/种群变化只标记为可见变化，不推断发生了几次移除、生成或变异事件。
- 低置信度、总活性不符、目标未知、缺后态、跨轮缺口均保留但列出不适用原因。
- 原型输入要求结构化六槽；缺失原始OCR字段不能用旧状态填成真值。原始OCR保存与人工修正界面留给下一阶段桌面采集适配。
- 不完整/中断牌局没有终局标签；不能拿最后一次F8的活性、手术难度奖励或奖励倍率当终局活性。
- 终局必须显式确认是第13轮常驻结算后的怪物总活性，协议使用`round=14, phase=terminal`，六槽乘积与显示总活性一致。该协议状态不是要求游戏UI实际显示第14轮。
- 时间戳未知的历史截图不伪造采集时间；以既有步骤顺序及画面来源记录，缺失边界继续标记。

目前完整终局学习资格较严格：从第1轮开始、相邻步骤由同一画面ID连贯、每步实际选择/目标/单次操作得到确认、已知常驻获得回合，并连接到可信终局。缺少这些条件的牌局仍可用于局部观察与待核对清单，不丢弃。

## 分类与统计

报告按真实/合成来源隔离，提供每场的常驻组合、各常驻、出现过的种群/稀有度、阶段/回合、槽位占用、零蛊虫人蛹场景，以及槽位清空/填入/属性变化标签。

候选出现次数统计全部完整候选，不只统计选中的牌；按发牌ID去重。出现次数不直接当作概率，因为条件牌、阶段和漏拍可能造成选择偏差。按整场得到的“出现过某种群”等标签只用于查找分类，不进入早期局面的学习输入，避免泄漏未来信息。

## 本次学习基线的意义

这是`episode-knn-v1`最近邻经验价值学习，不是神经网络、不是强化学习策略，也不是旧引擎评分表。新增真实经验可以改变估值，不需要手改卡牌强度。

输入仅为选择前可见的固定六槽数值/种群/稀有度与回合。同来源、游戏/规则版本、常驻及年龄、候选、阶段和重抽余量作为匹配上下文。保留左右槽位，不随机打乱。

目标标签是**已记录玩家策略下的最终活性**。这不能证明未选卡牌的反事实收益，也不能将预测直接当作最优策略。当前基线不推荐卡牌，下一步需要可靠模拟器或更完整的离线策略评测，才能评估不同动作。

按runId稳定散列到约70%训练/15%验证/15%测试，新增牌局不会改变已有分组。标准化只用训练数据；每场总权重一致；预测最多从每个训练牌局取一个最近邻，避免同局多次F8主导结果。训练/评测runId重叠、同局不同终局标签、重复样本均拒绝。

没有至少两场同上下文且足够接近的独立训练数据时拒绝估值。距离阈值是技术实验设置，尚未用实战校准。缺测试数据时报告`insufficient-data`，不是0误差。CLI拒绝把实战与合成数据混在一次训练里。

## 验证结果

- 原256项基线通过；本阶段新增40项单元测试，最终296项测试与生产构建通过。
- CLI真实子进程11项检查通过：验证/审计/归档/重试幂等/修订保留/列出/实战训练阻断/合成训练/旧文件防覆盖/非法输入/已有目录保护。
- 历史数据：1场、11步、11次候选、35个候选位置。保留11个可靠局部观察对；终局样本0。缺终局、部分连续画面和常驻获得回合，不生成终局标签。
- 3段已支持规则回放，逐槽与总数一致：68,328、331,980、463,722。其他8步标为未验证，不冒充模拟成功。
- 合成技术验证：40场完整虚拟轨迹、400个前态样本；28场训练、6场验证、6场测试。验证60行、测试60行均可估值；按场平均绝对误差分别22与30活性单位。这只是简单合成增长环境的流程结果，不是游戏推荐提升证据。
- 已验证改变训练标签会改变经验估值；JSON序列化/重载预测一致；不存在训练局到测试局的样本泄漏。
- 未训练生产策略、未启动在线学习、未安装/推送、未改字典或局面数据。无界面变化，前端构建产物仍相同，未进行新UI或物理F8验收。

最终证据目录：`work/learning-tech-verification-20260907-final/`；报告：`work/learning-tech-verification-final-report.json`。目录里实战和合成JSONL、合成模型分别保存。CLI验收临时目录以`vorax-learning-cli-`开头，保留便于检查；本次最终为`C:/Users/wuyin/AppData/Local/Temp/vorax-learning-cli-vRdozi`。

## 运行

```powershell
# 新目录/新文件：重复运行请换名字，不覆盖已有证据
pnpm learning verify --artifacts work/learning-verification-new --output work/learning-report-new.json

# 审计自然牌局JSONL（一行一场，含未完成牌局）
pnpm learning audit work/learning-tech-verification-20260907-final/historical.jsonl

# 以内容哈希保存不可变修订，ID不作为文件路径
pnpm learning archive 输入.runs.jsonl --directory 本地归档目录
pnpm learning list 本地归档目录

# 当前历史样本应返回blocked、退出码2，而不是伪造训练成功
pnpm learning train work/learning-tech-verification-20260907-final/historical.jsonl --output work/learning-real-attempt.json

# 技术测试，只使用临时目录
node scripts/test-learning-cli.mjs
```

归档使用`runId哈希-内容哈希.jsonl`。相同内容重试幂等；新修订与旧修订并存，不覆盖。同一runId的两个修订不可直接拼接后算成两场；分析前必须明确选一个修订。旧模型产物也不允许覆盖。

`work/learning-*`、`*.runs.jsonl`、`*.value-model.json`已加入gitignore。自选其他目录/文件名仍需自行避免提交；模型包含训练牌局样本，也是个人数据。程序不自动上传任何对局。

## 接入进度补记（2026-09-07）

客户端单次 F8/实际选择现已接独立事件归档，原始观察允许六槽不完整，查看/分类/暂停/导出入口已加入。详见 CLIENT_RECORDING.md。尚未安装、不自动训练，缺口/单步/终局/跨重启片段校订仍需后续完成。下方保留原离线阶段的接入路线，不再代表当前源码完全未接桌面。

## 原下一阶段路线：接入自然实战采集

本次尚不能在已安装客户端中自动归档每场。后续按以下顺序接入，而不是要求玩家控制随机结果：

1. 客户端增加可暂停的本地记录入口；“新牌局”明确创建runId，中途接入/退出另记不完整状态，不依赖回合数字猜换局。
2. 每次现有单次F8保存原始快照及本次可靠回填，不增加持续截图。重复画面去重，洗牌/药箱边界无法确认时保留未知。
3. 将现有点击选择证据关联到前后画面；缺目标、键盘确认、漏拍等列为待核对，不悄悄学习错误样本。
4. 分别记录额外常驻获得与培养回合推进；终局保存不掺手术方案奖励。
5. 增加按局查看、归类、核对、导出和安全独立落盘，再按保数据升级流程安装。

自然积累数据后，先检查完整率和常驻覆盖，再扩展规则样本、实战价值估计与策略学习。有限轨迹未显示某种效果，只能视为数据不足，不能认定这张牌没有效果。
