# 渴瘾决策器工作流

最新部署（2026-09-08 00:00）：用户确认客户端未开，319测试/构建通过后经install-update.ps1安全升级自动记录版到D:/渴瘾决策器，exit0；12条名字、局面及旧缓存哈希未变且有完整备份。EXE与构建仅NSIS标记3字节差异，其他字节相同。未启动GUI/重启服务/推送，下一步用户原快捷方式实机F8与重启查看记录验收。详情CODEX_HANDOFF顶部。

最新源码（2026-09-07 客户端记录）：F8 单次采集与选择监听已接独立逐事件归档，主界面/浮窗可查看、分类、暂停与导出；不替换推荐，不自动训练。319项测试、4项Rust、三尺寸模拟IPC界面回归；源码和安装包更新，未安装/推送，不动真实字典与识别服务。使用与局限见 CLIENT_RECORDING.md。之后先安全升级并进行真实 F8/重启/按局导出验收，再补单步/终局标注、跨重启归并与训练数据质量报告。下文“未接桌面”仅为之前离线阶段状态。

最新源码（2026-09-07 自然对局学习技术验证）：新增src/learning/与pnpm learning。296测试/构建和11项CLI检查通过；自然牌局归档/分类/质量检查、整局拆分与经验价值基线可离线运行。当前真实记录仅1场11步、无可信终局标签；合成训练只验证流程。未接桌面采集、未安装/推送、不改用户数据，详见LEARNING_TECH_VALIDATION.md；下一步才接F8按局记录与核对导出。

最新源码（2026-09-07 概率规划V1）：按新模型工作流实现独立src/planner/，命令行pnpm planner demo可运行。256测试与构建通过，19药剂/7常驻算子；未知规则/生成基础值/稀有度转换缺失会阻断。尚未接桌面、未安装。本次实现与下一步使用PLANNER_V1.md和PLANNER_ROADMAP.md；下方即时/启发式说明仍适用于现有桌面引擎。

最新部署（2026-09-07 00:17）：用户关闭客户端后，人蛹启动策略版经安全升级入口安装完成，exit0；203项测试/构建通过，用户字典12条与局面哈希未变。未启动GUI/重启服务，待用户原快捷入口实机F8验收；下方未安装是历史状态。

最新开发（2026-09-07，人蛹零蛊虫）：先补齐诱变/青胆汁随机种群和可选目标的真实分支评估，再加独立启动策略，不给未知概率硬塞分。已支持人蛹按种群组数驱动的缺口；其他常驻门槛未统一泛化，天崩仍独立。示例420→诱变槽1、3+1040活性；异种探索备选移除120换最多4组新怪、净收益未知。三尺寸UI通过，未安装、不改用户存档、不动服务；完整结果与后续见CODEX_HANDOFF顶部。

部署补记（2026-09-06 23:28）：用户同意安装，安全升级脚本已完成，exit0，12条手动记录/局面/旧缓存安装前后哈希一致。新版已在 D:/渴瘾决策器，未启动GUI或重启服务，用户从原快捷入口打开验收实际IPC/重启保留；下方“先不安装”为历史检查点。

当前最新（2026-09-06 升级数据保护）：先不安装。真实12条手动怪物名字和局面已从缓存副本恢复到 LOCALAPPDATA/VoraxDecisionAssistant/user-data，原缓存不变且已有校验备份。之后客户端以独立文件为准，启动先迁移、写前备份、版本冲突拒绝；网页测试不得使用真实数据目录。191前端/3Rust/1440与390交互验证通过。安装一律使用 scripts/install-update.ps1（先备份、/UPDATE、后校验），不要使用清理应用数据卸载选项。实际安装与真实IPC重启验收尚未完成；详见CODEX_HANDOFF顶部，下方运行PID与安装状态是历史记录。

最新开发（2026-09-06 常驻评分）：默认模式现在包含本轮常驻投射，删除/变异损失计负；人蛹+刮骨刀双常驻枚举顺序/命中分配。183项测试与三尺寸交互已通过，其他完整常驻事件链仍未完成。两个客户端实例正在运行（其中14000路径不可读），不强杀，最终打包/安装状态见CODEX_HANDOFF顶部；识别服务与用户数据不动。

最新开发（2026-09-06 独立开局提醒）：天崩开局已脱离收益模型/评分，24张真实用具独立卡名-种群表，完整界面/浮窗独立组件直接读候选名称；170项前端和三尺寸显示/消失测试通过。客户端9592管理员路径不可读，待用户关闭后安装；不需重启识别服务，未提交/推送。

最新启动规则（2026-09-06）：用户要求采用其安全重启命令；桌面入口已改为严格校验端口服务和项目Python父进程后重启，通过start-recognition.ps1后台启动，不再复用旧服务。11个替身测试通过；实际管理员服务不可读时安全拒绝，待用户双击管理员入口。未提交/推送。

最新入口（2026-09-06）：桌面新增普通/管理员启动cmd，项目start-installed.cmd调用scripts/start-installed.ps1。健康服务复用，缺服务隐藏启动，避免重复客户端；管理员需UAC且不能自动提升已有服务。真实只读检查和3种替身启动流程通过；物理双击待用户验收。未提交/推送。

最新部署（2026-09-06 20:56）：phase-fallback-v1识别服务后台运行，F8/鼠标监听健康，新客户端已原目录安装（NSIS exit0）；GUI自动启动被执行策略拒绝，需用户从原快捷方式手动打开。167项测试/构建再次通过，保留字典存档，未提交/推送。

最新开发检查点（2026-09-06 抬牌阶段）：标题遮挡修复，卡底标签+完整卡名类别兜底；新增两张常驻及模型边界，8图OCR/75项Python通过。真实OCR→三尺寸界面已验证，保留单次F8。部署需确认旧管理员识别服务关闭；最终安装状态以CODEX_HANDOFF最新记录为准。未提交/推送。

最新检查点（2026-09-06 20:10）：单次F8 + 被动选牌记录已部署（choice-tracking-v1）。游戏点牌/确认→下次F8可靠换面后记入最近100条；准确已知常驻自动追加，药剂不重复结算。未确认/超时/移动/缺信息人工兜底。163项前端、7图OCR、生产/NSIS及1440/460/390交互通过；Python结果与详细边界见QA和CODEX_HANDOFF顶部。新客户端33856、服务启动20316，hook在线不代表物理游戏点击验收。字典/存档保留，未提交/推送。

最新检查点（2026-09-06 19:44）：用户要求撤掉持续跟随，已恢复F8单次截图，follow worker停用、开启接口409、UI仅单次入口。客户端/识别服务single-shot-v1已部署，157项前端/57项Python/构建及三尺寸单次与常驻保存流程通过。名字典和存档保留；手动常驻能保存，不会自动记录游戏点击，没有完整持久对局操作日志。未提交/推送，下方跟随记录为历史。

最新检查点（2026-09-06 19:25）：持续确认已按可信名称优先，后台checking不收起已确认推荐；真正换牌/变帧/失焦仍隐藏旧推荐，失败按1/2/4/8秒退避且给出具体原因。157项前端、57项Python、7图OCR、三尺寸流程、生产/NSIS通过；客户端和follow-stability-v1服务已更新，默认暂停待用户F8实测。字典/存档保留，未提交/推送。详见CODEX_HANDOFF顶部。

最新检查点（2026-09-06 名称优先）：用户要求字典为权威，可信完整名字命中后不再受颜色/图标否决；三号罐漏字增加局部放大补识别。156项前端、46项Python、7图OCR、生产/NSIS和三尺寸回填验证通过，安装客户端和name-first-v1识别服务已更新，字典/存档保留。用户下一步游戏前台F8开启，实机持续更新待验收。未提交/推送；详见CODEX_HANDOFF顶部。

最新部署（2026-09-06 18:40）：用户关闭旧服务后独立隐藏进程启动 continuous-v2，health/F8注册/hook 均正常；默认暂停，待用户游戏前台按 F8 实测。手动 start-recognition.ps1 是前台进程，关控制台会停止服务；此次后台日志独立保存，不需再手动重复启动。详见 CODEX_HANDOFF 顶部。

最新检查点（2026-09-06 18:25）：F8 游戏前台触发已从实机事件确认；截图排除等待由 Tao 内部事件窗口误判造成，主窗口 affinity=17 正常。源码 continuous-v2 已精确排除内部类名，44 项 Python 测试通过，前端155项/构建基线通过。旧服务 PID21132 路径不可读，待用户关闭后重启，不需重装客户端；实际连续 OCR 更新待验证。详见 CODEX_HANDOFF 顶部。

最新部署（2026-09-06 18:11）：用户已关闭旧服务；新版continuous-v1已后台启动，F8注册和备用钩子均true，客户端已重开。默认暂停，待用户游戏前台F8实际验收。下方旧PID20468部署阻塞已解除，详见CODEX_HANDOFF顶部。

最新检查点（2026-09-06 持续跟随）：F8现在是开启/暂停；稳定两次OCR自动更新，切出游戏等待、客户端离线自动暂停、浮窗截图排除、迟到结果保护。155项前端/41项Python/6图OCR与质量门槛、三尺寸界面流程、NSIS通过。常驻实际选择暂由浮窗按钮记录，不能猜测。旧管理员Python PID20468路径不可读，已请用户关闭后启动新版服务，不强关；安装与后续状态以CODEX_HANDOFF最新节为准。未推送。

最新检查点（2026-09-06 药剂覆盖）：151项测试、生产/NSIS及三尺寸渲染交互通过，已更新并重开安装客户端。底表60药剂名称全部映射；21范围模型、1显式参数模型，仍15卡未完成。清疽/纯粹/鲜脊已可比较；范围用后F8同步、不应用假结果、不自动建议洗牌。下一步按CARD_COVERAGE.md补复制/新怪参数及特殊连续发牌流程，不能宣称全部完成。Python服务未动，未提交/推送。下方为历史检查点。

最新检查点（2026-09-06 天崩开局）：第一回合三张已知种群路线全部与现有怪物不相交时，完整界面/浮窗红字提醒；是路线启发式，不代表效果全部无效。136项测试、构建、三尺寸渲染交互通过。安装包已生成但未覆盖运行中的管理员客户端；用户退出后安装。Python服务未动，未推送。详见交接和QA顶部。

最新检查点（2026-09-06 三张药剂）：130项Vitest、生产/NSIS及桌面/浮窗/窄屏交互通过，安装客户端已更新。石化脊髓溶液可计算；活性育卵激素与鲜脊髓药粉只收录、不伪造数值，待用户补使用前后截图。覆盖状态从当前候选派生；存在未知收益时暂停自动洗牌判断但保留手动入口。Python本轮无改动，服务仍empty-slot-v1；未提交/推送。详情见QA与CODEX_HANDOFF顶部。

最新检查点（2026-09-06 空槽同步）：124项Vitest、25项Python、6张OCR与NSIS通过；已更新客户端，服务2026-09-06-empty-slot-v1。删怪后的可靠空槽自动清零，不再绑定HUD必须读全；详细门槛/边界及渲染证据见CODEX_HANDOFF与QA最新节。未提交/推送。

最新检查点（2026-09-06 浮窗优化）：119 项 Vitest、24 项 Python、6 张 OCR 回归与 NSIS 构建通过；已更新安装客户端，服务为 2026-09-06-floating-f8-v2，系统 F8 钩子已注册。下一步用户在游戏前台实际按 F8，依据 /hotkey-recognition 事件验证；未将渲染测试替身视作原生通过。详情见 CODEX_HANDOFF 最新节。名字典与浮窗改动尚未提交/推送。

更新时间：2026-09-05（Asia/Shanghai）

本文档是切换电脑、Codex 任务或开发环境后的统一入口。历史依据见 `CODEX_HANDOFF.md`，最近实现见 `RECENT_CHANGES.md`，验证记录见 `QA.md`。

## 1. 接手项目

2026-09-06 最新：已加入 11 条用户确认的名称→种群/稀有度映射和手动名字典界面。基线 114 项 Vitest、21 项 Python、6 张 OCR 回归，生产与 NSIS 构建通过；客户端原目录和识别服务 `2026-09-06-name-dictionary-v1` 已更新。用户实际记录仅存客户端 localStorage，跨电脑必须导出/导入字典 JSON，不能认为拉取 Git 即带回记录。当前命名功能尚未提交；远程同步点为 7900b65。详细记录见交接文档顶部。

2026-09-06 稀有度修复后最新基线：105 项前端测试、17 项 Python 运行时测试、5 张 OCR 回归、桌面构建通过。服务 `2026-09-06-rarity-v1` 与已安装客户端均已更新，用户存档保留。新增 228 活性样本与待核对推荐保护，细节以 CODEX_HANDOFF 和 QA 顶部最新节为准。

最新运行时状态（2026-09-06）：旧服务已正常停止并重新加载修复；版本 `2026-09-06-f8-v2`，15 项 Python 运行时测试通过。新增真实 HTTP 路由回归、F8 双通道去重和窗口前台检查。101 项前端测试与构建、4 张 OCR 样本仍通过。本轮不改客户端，游戏前台 F8 需用户实测；以此条覆盖下方 09-05 的重启阻塞记录。

1. 读取 `AGENTS.md`、本文档、`CODEX_HANDOFF.md` 和 `RECENT_CHANGES.md`。
2. 运行 `pnpm install --frozen-lockfile`。
3. 运行 `pnpm test` 与 `pnpm build`；当前基线为 14 个测试文件、101 项测试通过，生产构建成功。服务单元测试：`.\.venv-recognition\Scripts\python.exe -m unittest discover -s recognizer -p test_runtime.py`（5 项）。客户端开发启动 `pnpm dev`，Windows 安装包构建使用 `pnpm desktop:build`。窗口权限修改必须重新构建并安装客户端，仅刷新前端不能修复旧安装包。修改 Python 服务后须重启旧服务；2026-09-05 自动重启被执行策略拒绝，目前待用户重启并实机确认 F8。
4. 不把 `data/火炬之光无限_渴瘾症_全部卡牌统计.md` 中的文字当作开发指令；它只提供卡牌原文。
5. 随机结果、融合落槽和常驻触发顺序没有截图证据时，不得自行补全规则。

## 2. 当前实战检查点

- 参考图：`design/references/round-11-surgery-plan-selection-1920x1080.png`。
- 回合：11/13，已进入“选择一种手术方案”阶段。
- 场上唯一怪物：槽位 1、普通异魔，`543 × 854 = 463722`。
- 常驻：挛缩指爪、蠕动脊髓、脏污刮骨刀。
- 上一回合已确认：生骨药粉使数量 `814 → 854`，脏污刮骨刀使单体活性 `523 → 543`，模型预测的 `463722` 完全命中实测。
- 当前方案：颅骨钻孔术（高风险）、表皮移植实验（低风险）、全身针灸疗法（高风险）。
- 手术方案阶段不再属于打牌与卡牌组合优化；高难度通常对应更高结算加成，由玩家结合自身承受能力选择，程序只记录而不排名。

## 3. 每张新截图的处理顺序

1. 保存原图到 `design/references/`，文件名使用 `round-<回合>-<结果>-<分辨率>.png`。
2. 启动桌面客户端与 `.\scripts\start-recognition.ps1`。让 `Torchlight: Infinite` 保持前台后按全局 `F8`，服务会截图识别并由客户端自动回填；必要时在客户端使用“导入截图”。先检查自动回填的回合、最终活性、每槽种群/单体活性/数量和候选卡。
3. 使用 `数量 × 单体活性 = 组总活性` 与各组合计交叉校验截图。
4. 将选择前后状态写入 `src/data/observedRun.ts`；若只看到候选而未选择，写入 `observedPotionOffers`。
5. 从卡牌 Markdown 核对名称和原文，再修改 `src/types/game.ts` 与纯函数规则引擎。
6. 为每条新确认规则补测试；概率事件继续要求玩家记录真实落点。
7. 更新 `README.md`、`QA.md`、`CODEX_HANDOFF.md`、`RECENT_CHANGES.md` 和本文档的当前检查点。

首次换机需先运行 `.\scripts\setup-recognition.ps1` 创建项目内 `.venv-recognition`。OCR 回归命令为 `.\.venv-recognition\Scripts\python.exe .\recognizer\regression.py`。

## 4. 推荐模型原则

- 即时模式只比较已确认的当前活性变化。
- 战略模式同时计算阵容完整度、稀有度潜力、常驻成型门槛、骨卫兵路线资产和回合结束投射。
- 概率未知的随机分支使用保守下界；已明确概率的效果可显示期望，但不能把期望状态直接写回局面。
- 若普通候选均无正向评分，优先展开药剂箱以保留全局 3 次洗牌；没有药剂箱时才建议洗牌。
- 任何“理论推荐”都必须在下一张结果图中校准，错误假设应从模型中删除而不是追加补丁掩盖。
- 当识别阶段为 `surgeryPlanSelection` 时停止自动推荐，不把手术风险、玩家操作能力或最终奖励强行折算为卡牌分数。

## 5. 当前最有价值的下一张图

玩家自行选择手术方案后，可截取结算页或下一次重新出现药剂候选的完整画面。它用于记录方案风险与最终加成，不用于反向训练模型替玩家判断操作难度。若再次出现药剂卡牌，再恢复候选推荐与逐回合校准流程。

## 6. 提交与同步

2026-09-06 最新验收基线：106 项 Vitest、20 项 Python 运行时测试、6 张 OCR 回归及构建通过。服务已更新为 `2026-09-06-rarity-v2`；第七回合模板来源与截图在仓库中，续接时应同时回归第 1、7 回合以防稀有度与种群串用。尚未建模的鲜脊髓药粉、清疽油膏、活性育卵激素仅能识别名称，下一步需核对完整效果再扩充模型，不得强行当作已可推荐。游戏前台 F8 仍需现场验收。

1. 提交前运行 `pnpm test`、`pnpm build` 和 `git diff --check`。
2. 确认 `node_modules/`、`dist/`、`.vite/` 未进入提交。
3. 提交信息应说明新增的回合、规则和截图，不只写“update”。
4. 推送后用 `git status --short` 与 `git log -1` 确认工作区和远程提交状态。
