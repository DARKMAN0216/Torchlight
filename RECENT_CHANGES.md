# 近期修改整理（2026-09-03 至 2026-09-04）

## 2026-09-07 客户端单次 F8 自动记录

- 接入原始识别、六槽质量校验、全部候选、实际选牌状态与常驻获得证据；不修改推荐计算。
- 主界面/浮窗新增牌局记录、暂停/恢复、分类查看、原始/学习档案导出；新局、导入截图、重启片段分开处理。
- 独立不可变事件存储；保存失败显式告警、保留内存事件、可重试与备份。不与字典/局面存档混用。
- 自动数据仍需单步、随机落点和终局校订后才能训练；未知不是零收益。源码已接入，安装版待升级。详见 CLIENT_RECORDING.md。

## 2026-09-07 自然对局归档与学习技术验证

- 新增独立learning原型：按场记录/分类、完整候选统计、未知与低可信保留、终局标签保护、不可变修订归档及JSONL审计。
- 最近邻经验价值学习跑通数据集→按局拆分→训练→独立评测→模型保存；不把人类选择当最优，不用合成标签补真实缺失终局。未接桌面记录或推荐。
- 296项测试/构建与11项CLI检查通过；已有1场11步归类但无终局训练样本，3段规则回放一致；40场合成仅验证学习管线。详情LEARNING_TECH_VALIDATION.md，用户存档/服务/安装状态不变。

## 2026-09-07 概率规划原型与训练路线

- 新增独立纯函数src/planner/和JSON命令行：13轮模拟、合法动作、事件连锁、重抽、常驻延迟、经验稀有度、Monte Carlo/动作Beam及顺序情景。
- 接入19张药剂/7张常驻算子；明确数据缺口和未迁移牌，不使用固定卡牌强度分，也不把未知牌算零收益。
- 提供RecognitionSnapshot严格适配、真实结果校准、训练资格检查和JSONL；未接桌面UI或自动日志落盘。
- 256项测试、生产构建与CLI演示通过。后续方案包含规则/概率采集、独立留出评测、价值模型加速与部署门槛，见PLANNER_ROADMAP.md。

## 2026-09-07 常驻缺口与启动路线

- 修正诱变药粉/青胆汁溶液缺少实际随机种群时跳过目标评估的问题：先优化可选目标，再枚举随机结果，不把随机结果当成可选项。
- 人蛹零蛊虫：常驻协同模式加入启动优先偏好，数值与路线分开；确定优先、随机不保证、不盲目承担已知净损失、已有蛊虫与末轮退出。
- 异种激素未知净收益不再从策略讨论消失：展示最低移除损失、容量与探索机会，继续排除数值排名和自动结算。
- 真实420案例推荐诱变槽1、3（显式活性+1040），主界面/浮窗显示目标、替代路线与风险。未安装/未推送，用户数据不改。

## 2026-09-06 升级保留手动怪物数据

- 字典及局面从仅 WebView 缓存改为独立用户目录文件；启动先读/迁移，修改前备份、原子保存，读取失败暂停，旧客户端冲突不能覆盖新记录。
- 成功写盘后才显示名字典保存成功；设置显示实际存储目录。网页预览明确仅浏览器保存。
- 已找回12条原手动记录及当前局面并独立保存，原缓存完整保留且另有哈希校验备份。
- 安全升级脚本先备份再 /UPDATE，更新后校验用户文件。191项前端/3项Rust测试与1440/390交互验证通过；未安装、未提交推送。

## 2026-09-06 默认模式计入常驻

- 各模式计入已支持常驻的本轮结算增减，常驻收益损失不再被即时加成掩盖；完整界面和浮窗分解显示，不修改实际怪物投射状态。
- 人蛹/刮骨刀双常驻覆盖随机分配与结算顺序；新常驻周期按下一回合、已存在常驻不重复加分；移除可同时计算多张已支持触发卡，跨门槛不猜。
- 183项测试通过；其他多常驻完整事件链仍待验证，未知分支不自动建议洗牌/药箱。当前安装状态见交接顶部。

## 2026-09-06 天崩开局脱离收益模型

- 建立24张手术用具独立种群关联表；修复人蛹标本无strategicProfile导致整组提醒消失的问题。无评分/未建模也可以按名称提醒。
- 首轮三张关联种群与场上种群完全无交集显示红字，只判断种群关联，不判断稀有度、触发门槛或收益。通用/未知卡与不可靠局面不武断提醒。
- 提醒移出推荐组件，在完整界面和浮窗独立展示；保留“不代表效果无效”的说明，不改变排名。170项测试和三尺寸交互验证通过，部署状态见交接顶部。

## 2026-09-06 抬牌遮挡后的常驻阶段修复

- 快速OCR补卡底类别区域；标题/类别标签与完整卡名类别交叉验证，冲突保持unknown，不靠回合数猜阶段。用户408活性截图三张常驻正确识别，选牌监听可启用。
- 斑斓肝脏、人蛹标本结构化入库，支持手动追加与点击记录校验后追加。肝脏在变异前后均满足双异魔时触发；跨门槛与新增变异连锁时点待验证，暂不计该条件收益。人蛹标本仅作X至X+1轮范围投射，不写回随机结果。
- 常驻卡评分明确为路线启发式而非活性期望；常驻选择阶段不再显示药剂池洗牌建议。红瘟三头犬仍为异魔首领，不满足脑垂体的觉醒者条件。
- 保留单次F8、原存档与名称字典；部署状态见CODEX_HANDOFF顶部。

## 2026-09-06 单次F8 + 选牌记录

- 游戏前台点牌、点确认；下一次F8校验换面，记录实际点击线索。已知常驻自动加入后续药剂协同计算；药剂不重复执行，结果仍以截图为准。
- 最近100条日志与常驻同存，支持暂选/待核对/人工确认/误记提示及暂停监听。3/5牌、改选、药箱同轮、刷新失效、窗口移动/超时、重复/刷新持久化已覆盖测试。
- 仍是单次OCR，不恢复持续模式；不是游戏内部选择回执，具体游戏鼠标流程需要实测。已更新本机客户端及服务，存档和字典保留；未提交/推送。

## 2026-09-06 撤掉持续跟随，恢复单次

- 用户实测更偏好单次：F8每按一次截图识别一次，不再循环观察；完整界面/浮窗入口统一，保留名称字典、短名称补识别和目标提示。
- 常驻用具可手动选择并自动保存在局面中；游戏内真实点击仍无法自动知道，尚无完整持久化选牌日志。
- 157项前端、57项Python、生产/安装包与三尺寸单次/常驻保存测试通过。客户端及服务已更新，字典/存档保留。

## 2026-09-06 持续识别循环与复查闪烁

- 持续确认也采用可信名称身份，图标/颜色波动不再反复推翻同一怪物；不一致视觉属性不会被当作未知名称的已确认兜底。
- 后台checking复查保留当前推荐；真实变化/失焦/不完整仍隐藏旧结果并重新确认。失败按1/2/4/8秒降频，显示具体原因及重试次数。
- 157项前端、57项Python、7图OCR、三尺寸流程及安装包通过；客户端/服务已更新，字典和存档保留，待用户F8实战验证。

## 2026-09-06 已录入名称直接决定怪物属性

- 根据用户要求，可信完整名称命中字典后直接回填种群/稀有度，图标和颜色不再否决；手动同名覆盖优先，未知/低可信名字仍谨慎处理。
- 修复第5回合三号罐蝎兽短名称漏检：有数值证据且名字缺失时局部3倍放大补读，原图读出约99.86%置信度，不硬编码槽位或名字。
- 156项Vitest、46项Python、7图OCR、三尺寸界面回填和生产/NSIS通过；客户端与name-first-v1服务已更新，存档/字典未清除。实机持续跟随待用户F8验证，未提交/推送。

## 2026-09-06 从单次F8切换为持续跟随

- F8开启/暂停后自动观察稳定局面，读取怪物、回合、三/五张新牌和可靠洗牌次数；不重复结算模拟结果。动画/切出游戏时暂停旧推荐，重复帧不刷历史。
- 跟随期间浮窗启用截图排除，无需反复隐藏；暂停恢复可截图。关闭客户端15秒后自动停止观察。单次识别保留，导入图片先暂停。
- 浮窗提供已选常驻确认按钮；右上角常驻图标尚未自动识别，中途接入仍需核对常驻组合。155项前端/41项Python/6图回归、界面及构建通过；旧管理员服务待用户关闭后重启，详见交接。

## 2026-09-06 药剂范围模型与缺口审计

- 清疽油膏、纯粹活蛭溶液不再未匹配，鲜脊髓药粉支持变异/数量协同范围；扩展多个随机/移除复合模型。底表60药剂名称均已映射，但仍15卡未完成数值模型，不按零收益处理。
- 育卵激素使用设置中新蛊虫显式参数；范围卡只能预览，实际游戏选牌后F8回填。当前候选有范围/未知收益时暂停自动洗牌判断；卡库可筛选缺口，完整清单见CARD_COVERAGE.md。
- 151项测试与生产/NSIS/三尺寸交互通过；安装版已更新重开，存档保留，Python服务未动，未提交/推送。

## 2026-09-06 天崩开局

- 新增第一回合三张常驻用具的种群路线完全不匹配红字提醒；完整界面与浮窗共用，当前状态改变即重算。不把路线不匹配当成所有效果无效，不限制用户选择。
- 未知/通用用具、空阵容、识别待核对不误报；离开第一回合或出现匹配种群自动消失。136项测试、生产/NSIS、渲染通过；安装包待用户关闭当前客户端后安装，服务与存档未动。

## 2026-09-06 三张药剂收录

- 三张卡名匹配：石化脊髓溶液已支持即时结算、最佳目标与孽生肉芽协同；活性育卵激素、鲜脊髓药粉仍待量化，显示原文及缺少依据。
- 未量化不等于零收益：禁止直接应用、排除数值排名，当前候选含未知收益时暂停自动洗牌建议，保留手动洗牌。浮窗同步提示比较范围。
- 130项测试、生产/NSIS、桌面/浮窗/窄屏交互通过；安装客户端已更新，未清除存档，识别服务无改动。尚未提交/推送。详见QA与CODEX_HANDOFF。

## 2026-09-06 空槽自动确认

- 删怪/融合后的可靠空槽独立清零，避免HUD漏读或另一槽缺数值时，所有空槽一起要求确认。
- 完整游戏画面、空槽置信度和无矛盾内容作为保护；空白/缺槽/模糊图不自动抹掉旧怪物，可信总活性矛盾仍保留核对。
- 未知名字也是非空证据；非空槽缺失数值只提示该槽。124项前端、25项Python、6图回归及安装构建通过，客户端/服务已更新。详见交接和QA。

## 2026-09-06 浮窗使用优化

- 460×680 浮窗：六槽怪物、稀有度颜色、当前识别名字、活性×数量/总量；明确推荐目标槽位并高亮，随机命中不伪装为可选目标。
- F8 新增独立系统按键备用监听，与已有热键/轮询去重；浮窗显示请求是否到达和识别耗时，恢复焦点主动更新。
- 119 项前端/引擎测试、24 项 Python、6 图回归、渲染交互与 NSIS 通过；已更新客户端和服务。实际游戏 F8 仍待用户验证，根因尚未确认，未自动推送。详见 CODEX_HANDOFF / QA 最新节。

## 2026-09-06 名称字典与手动记录

- 内置用户明确提供的 11 条完整名称映射：寄生蝴蝶/蛊虫/稀有、刚盾卫兵/骨卫兵/稀有、禁典学者/觉醒者/稀有、邪眼异兽/异魔/稀有、堕落者与朝圣者/觉醒者/魔法、裂颚兽/异魔/魔法、轻弩兵/骨卫兵/魔法、蝎兽/异魔/普通、腐囊异兽/异魔/稀有、赤螳螂/蛊虫/魔法。同属性可以对应多个名字；邪眼翼兽与邪眼异兽不擅自合并。
- Python 返回名称区所有完整 OCR 名字（包括未入库的学者、智者），不再只返回旧名字表命中项；种群旧兜底改为完整名称匹配。客户端在 merge 层进行字典查询，不耦合游戏规则引擎。
- 名称可信度至少 0.85 且完整命中时补全种群和稀有度；高可信（至少 0.80）的图标/颜色冲突进入 recognitionReview，不能因命中字典而绕过核对。未知或低可信名字保留视觉兜底，不做子串或错字猜配。
- 顶部“怪物名字典”显示未收录槽数。弹窗展示本次截图的原始名称、视觉属性和可信度；点击带入名字，手动明确选择属性。保存仅对下一次 F8/上传生效，不把旧截图重新覆盖当前局面。
- 手动字典采用独立版本化 localStorage 键 `vorax-monster-dictionary-v1`，新牌局/撤销不清空；提供搜索、编辑、删除、JSON 导入导出。同名属性冲突须明确勾选覆盖；导入先完整校验再一次保存。写入失败不更新内存或报成功；损坏存储保留原文并提供原始备份导出。
- 跨设备：源码 Git 不会自动同步客户端手动记录，离开旧电脑前导出 JSON，到新客户端导入。网页开发预览与桌面客户端是不同存储来源，QA 中的“学者”仅是测试数据，没有写入用户安装版字典。
- 验证：114 项 Vitest、21 项 Python、6 张 OCR 回归、TypeScript/Vite/NSIS 构建通过。服务运行 `2026-09-06-name-dictionary-v1`；已静默更新原安装目录 `D:\渴瘾决策器`（退出码 0）并启动客户端，未清除原存档。
- React/前端测试技能影响：字典读取惰性初始化、版本化且失败不假报成功；查询/未收录计数从当前数据派生。Browser plugin not available，项目未装 Playwright，使用已有捆绑 Playwright+Edge，无新增依赖。
- UI QA：`http://127.0.0.1:5174/`，1440×1000 与 390×844。真实原图上传→学者待记录→明确选属性并保存→刷新/新牌局保留→导出/删除/导入恢复→同名冲突拒绝与显式覆盖→模拟配额失败不改记录→下一次缺失视觉属性的快照由名字补全→视觉冲突暂停→Escape 关闭。后两个识别分支采用真实 OCR 响应的测试变体，不代表游戏 F8 实测。
- 页面身份、非空、无框架错误层、交互、桌面/窄屏截图、无横向溢出通过；修复截图发现的默认白底按钮不可读。控制台仅开发站点 favicon.ico 404，无其他错误。证据：`%TEMP%/vorax-monster-dictionary-desktop.png`、`vorax-monster-dictionary-mobile.png`，脚本 `vorax-monster-dictionary-qa.cjs`。
- 边界：实际游戏前台 F8、安装版原生文件下载/上传对话框仍需用户实测。三张未建模药剂问题未在此轮扩展。此次名称功能修改尚未提交/推送；上一次远程同步点为 7900b65。

## 2026-09-06 第七回合漏识别修复（rarity-v2）

- 复现原因：实时 OCR 将种群图标误读为“乐/常”，与名称同时进入取色候选，导致稀有度被拒绝；第二槽觉醒者图标与旧模板的匹配不足。名称候选带下界从 0.347 调整为 0.355，排除图标所在行；补充用户原图的异魔/觉醒者模板变体，仍保留 0.80 门槛并要求跨种群分差至少 0.08，不通过降低可信要求掩盖问题。
- 修正“活性育卵激素”被通用“活性”关键词过滤的问题；标题允许该词，带数值的效果正文不作为标题。
- 原图固化为 `design/references/round-7-rarity-icon-regression-2560x1440.png`。四槽为稀有异魔 191×581、普通觉醒者 32×36、稀有觉醒者 162×36、魔法骨卫兵 5×49；两空槽，总活性 118200。模板裁剪来源见 `recognizer/templates/monster-races/README.md`。
- 验证：106 项 Vitest、20 项 Python 测试、6 张 OCR 回归及 TypeScript/Vite 构建通过。重启后的真实 HTTP 接口上传该图耗时约 1639ms，四槽种群/稀有度、三个卡名正确，diagnostics.issues 为空。合并测试验证完整可靠的新快照可恢复第二槽并清除旧待核对标记。
- 服务已重启为 `2026-09-06-rarity-v2`，健康检查显示 F8 注册与备用检测在线。此次仅生产 Python 代码变化，无需重装客户端；不等于游戏前台 F8 已实测通过。
- 边界：鲜脊髓药粉、清疽油膏、活性育卵激素已读出名称，但均未接入结构化效果模型，不能给出可信排名；这是独立覆盖缺口。黄色稀有度已有本次实图验证，红色首领仍缺实图；同源模板回归不代表不同分辨率/HDR/动画条件全面验收。

## 2026-09-06 怪物稀有度同步修复

- 新样本 `design/references/round-1-rarity-sync-2560x1440.png`（用户原图）：四槽依次为骨卫兵普通、骨卫兵普通、觉醒者普通、觉醒者魔法；数值 36×1、36×1、36×1、24×5，总活性 228。
- 后端在 OCR 名称框内排除上下留白，对明亮文字像素做 HSV 投票，输出独立 rarity 字段，不将红眼种群图标或红色活性图标当作首领。只用名称定位，不依赖怪物名字典。种群图标置信度也纳入 occupied 门槛。
- 前端回填可靠稀有度。观测到普通/魔法的新怪物可覆盖旧存档首领，这不改变规则引擎的首领升阶上限。
- 稀有度/种群不可靠或槽位不完整时，将 recognitionReview 随局面保存；完整界面和浮窗暂停推荐。可重拍或在左侧修正后明确点击“已核对怪物信息，恢复推荐”。单纯刷新、改回合、修改其他字段不会静默解除待核对状态。
- 额外修正肿大脑垂体的战略启动条件：groupThreshold 支持 rarities 集合，只有稀有/首领觉醒者满足；原本仅检查种群会错误加 84 分。本样本回合结束收益与战略增分均为 0，推荐挛缩指爪 +149。
- 验证：105 项 Vitest、17 项 Python 运行时测试、5 张 OCR 回归、TypeScript/Vite 与 NSIS 构建通过。
- 界面验证遵循 React/前端测试技能：Browser plugin not available；使用已有捆绑 Playwright + Edge，未新增依赖。`http://127.0.0.1:5174/`，1440×1000 / 430×340 / 390×844。真实 HTTP 上传用户原图 → 覆盖旧首领稀有度 → 肿大脑垂体零收益 → 构造缺失稀有度结果 → 阻止推荐 → 刷新仍保留核对 → 浮窗提示 → 人工确认 → 真实重拍恢复。原生窗口API为测试替身，未用模拟测试声称游戏快捷键通过。
- 页面身份、非空、无框架错误层、交互、截图和 390px 无横向溢出通过；仅开发站点 favicon.ico 404，与识别无关，无其他控制台或运行时错误。证据在 `%TEMP%/vorax-rarity-fixed-desktop-20260906.png`、`vorax-rarity-review-float-20260906.png`、`vorax-rarity-fixed-mobile-20260906.png`，脚本在 `%TEMP%/vorax-rarity-fix-qa-20260906.cjs`。
- 服务已重启加载 `2026-09-06-rarity-v1`；安装包已静默更新到原目录 `D:\渴瘾决策器` 并重开客户端，未清除用户存档。安装 exe 与构建 exe 仅三个字节的 Tauri 包类型标记不同（NSS/UNK），其余二进制一致。
- 边界：新图实证普通/魔法；黄/红分类仅有合成颜色单元测试，不等于真实稀有/首领截图验收；亮度、HDR、不同 UI 布局仍需采样。后台浮窗免切窗方案不在本次改动范围内。


## 2026-09-06 F8 运行时修复（待游戏实测）

- 对照测试：游戏前台请求序号停在 3，决策器前台变为 11，但因避让后的前台检查失败而取消；不能将注册成功等同于游戏中可靠接收，也未证明管理员权限是根因。
- 修正 `/trigger-capture` 被错误放入 `log_message` 导致响应递归的问题，路由移回 `do_POST`，按钮事件单独标为 `capture-button`。
- 保留 RegisterHotKey，增加仅 F8/修饰键状态的 10ms 备用检测；仅游戏或决策器前台的无修饰 F8 下降沿可触发，双通道去重、长按不连发。注册冲突时不启用备用通道，避免抢占他人快捷键。
- 避让改用 SetWindowPos 的隐藏/不激活标志；决策器前台发起截图时仅请求 Windows 聚焦已验证的游戏窗口，再检查焦点。焦点被系统拒绝或转到第三方应用时取消，不注入按键、不提升权限、不绕过游戏保护。
- 最新结果增加 triggerSource、pollingActive、最近 30 条事件；elapsedMs 完成后停止增长。窗口前后句柄与避让列表记录在本地服务日志，不记录普通输入文字。
- 101 项前端测试及构建通过；15 项 Python 测试包含真实 HTTP 路由、去重与焦点安全分支；4 张 OCR 参考图全部通过。本次无前端改动，无需重新安装客户端。
- 旧服务已由 Codex 正常停止并加载修复版，健康版本应为 `runtimeVersion: 2026-09-06-f8-v2`。当前仍需用户真实游戏前台 F8 验收，备用检测也可能受 Windows 权限/桌面隔离限制，不能提前声称彻底解决。


这份文件只记录最近一段实战校准与实现变更；完整历史仍见 `CODEX_HANDOFF.md`、`QA.md` 与 `SCREEN_RECOGNITION_PLAN.md`。

## 2026-09-05 识别与浮窗联动

- OCR 改为原像素区域拼图，四张样本回归通过；第 10 回合约 2.9s → 1.8s（不同游戏负载下会变化）。
- 修复 F8 被浮窗抢焦点后遭前台检查拒绝的问题：截图时本程序窗口短暂避让，恢复不抢焦点；其他应用仍不捕获。
- F8 注册与工作状态显式回传，服务重启不再因序号倒退而丢结果；截图按钮发起实际请求。
- 浮窗接上常驻排名；药剂未匹配说明原因且不沿用旧卡推荐。101 项前端测试、5 项服务测试通过。
- 安装包已重建，旧识别服务重启被执行策略拦截，待用户手动重启后进行真实游戏 F8 联动验收。

## 2026-09-05 浮窗修复

- 补齐 Tauri main 窗口的置顶、尺寸和最大化相关权限，修复客户端误报“网页预览不支持置顶”。
- 使用真实桌面环境检测，权限/系统失败显示原始原因；进入中途失败尝试还原窗口。
- 保留物理像素尺寸与原最大化状态，防止高 DPI 退出后放大；切换按钮增加异步操作锁。
- 新增 6 项窗口/权限回归，当前 94 项测试通过。原生游戏置顶与 F8 联动需用新安装包实机验收。

## 已确认并已实现的规则

1. 融合包含“移除、添加、融合”三个机制。
2. 融合会先移除所选组，再把新组加入最左侧空培养皿；不会保留原目标槽位。
3. 融合移除非骨卫兵会触发挛缩指爪；第 7 回合这次因场上仅剩融合组，`+150 数量` 落点确定。
4. 融合添加新组会触发蠕动脊髓；第 7 回合本次成功，融合组变异为异魔并 `+80` 单体活性。
5. 脏污刮骨刀会在该回合结束后，对数量大于 275 的组再 `+20` 单体活性。

实战链条：

`146 × 316` 与 `176 × 194`
→ 融合 `322 × 510`
→ 挛缩指爪 `322 × 660`
→ 蠕动脊髓成功 `402 × 660`
→ 脏污刮骨刀 `422 × 660 = 278520`

## 实现变更

- `src/engine/evaluate.ts`
  - 融合改为“清空所选组后，写入最左侧空槽”。
  - 融合会向运行时同时发出移除组与新增组事件，让两类常驻都参与结算。
  - 新增 `observedAddedGroupMutationIdsByCardId`，可把截图确认的蠕动脊髓成功/失败结果写回真实局面，而不是只显示期望值。
- `src/types/game.ts`
  - 扩展 `EvaluationContext`，承载新增怪物的实际常驻变异结果。
- `src/data/sampleLibrary.ts`
  - 新增普通“药剂箱（小）”，与“稀有药剂箱（小）”分开；两者均展开 3 张，但仅稀有版本带高稀有度倾向。
- `src/data/observedRun.ts`
  - 已加入第 7 回合融合至第 8 回合，以及第 8→9 回合消化酶、第 9→10 回合强效祛异药粉的可重放链条。
- `src/data/sampleLibrary.ts`、`src/App.tsx`、`src/engine/evaluate.ts`
  - 强效祛异药粉改为“至多移除 2 组”：候选不足时只移除实际存在的不同种群；当完全没有候选时，仍可确认其 `+154 数量` 主效果。
- `src/data/sampleLibrary.ts`、`src/types/game.ts`、推荐界面
  - 孪生激素-蛊虫接入已确认的“所选组变为蛊虫”，新增同名组保持未计分并提示所缺参数。
  - 混合活蛭溶液登记为规则待确认卡；仅一组同稀有度怪物时暂按 0 个已确认收益参与保守排名。
  - 新增模型覆盖度提示；含不完整卡牌时不再把结果表述成无条件的局部最优。
- 测试
  - 覆盖融合落槽、移除/添加常驻连锁、实际变异写回、两种小药剂箱和第 8 回合推荐。
  - 新增第 10 回合三张候选的完整映射、保守排名与 `463722` 终态投射。
- 第 10 回合生骨药粉结果已加入可重放轨迹，预测的 `523 × 854 → 543 × 854 = 463722` 与实测完全一致。
- 新增 `surgeryPlanSelection` 识别阶段及 `decisionAuthority: 'player'` 边界，明确手术方案不进入卡牌推荐。
- 本地屏幕识别第一阶段已落地：RapidOCR/ONNX Runtime 一次整图推理，网页可识别主屏幕或导入截图，并按置信度自动回填。
- 第 1、4、10、11 回合代表性截图 OCR 回归通过；三卡、五卡、怪物数值、最终活性和阶段均可识别，单图约 1.5–2.4 秒。
- 重抽估算改为精确组合计数，不再物化几十万个牌组；结果与暴力枚举一致。
- 当前基线为 11 个测试文件、87 项测试全部通过；生产构建成功。
- 本机恢复 OCR 环境时确认 `onnxruntime >= 1.23` 与 Python 3.12 不兼容，因此初始化脚本固定优先使用 Python 3.11；项目虚拟环境已用 3.11 验证运行。
- 直捕曾误读 Codex 覆盖后的主显示器内容；现改为定位 `torchlight_infinite.exe` / `Torchlight: Infinite` 的客户区，并要求游戏前台，否则安全报错。
- 本地服务现注册全局 `F8`：游戏保持前台时即可在后台截图识别，网页通过本地轮询自动回填最新结果；网页按钮仍可能遮住全屏游戏，因此保留“导入截图”兜底。
- 已将正式入口封装为 Tauri Windows 客户端：`pnpm dev` 启动原生窗口，`pnpm desktop:build` 生成 NSIS 安装程序；`pnpm web:dev` 仅保留给开发调试。客户端窗口使用 1440 × 920 默认尺寸，并保留本地 F8 识别与截图导入工作流。
- 所有手术阶段会明确跳过手术卡，避免显示为“未覆盖当前候选牌”。

## 当前实战局面（第 11 回合，手术方案）

- 场上唯一怪物：槽位 1、普通异魔，`543 × 854 = 463722`。
- 当前为颅骨钻孔术（高风险）、表皮移植实验（低风险）、全身针灸疗法（高风险）三选一。
- 按用户确认，高难度通常对应更高加成；这是玩家操作与风险偏好决策，模型停止自动排名。

## 尚待确认，不应当伪造

1. 蠕动脊髓的失败分支截图，以及触发动画的实际顺序。
2. 生骨药粉命中骨卫兵后的额外 `+127`、移除和挛缩指爪连锁。
3. 孪生激素-蛊虫新增“同名怪物”的初始数值、种群与落槽规则。
4. 混合活蛭溶液在同稀有度怪物不足两组时的实际表现。
5. 普通药剂箱（小）展开后的具体三张牌。
