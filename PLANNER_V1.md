# 概率规划模块 V1：实现、运行与边界

日期：2026-09-07。依据《渴瘾症卡牌决策模型工作流.md》新增。

这是已可运行和测试的规则模拟、概率搜索及校准接口原型。代码位于现有项目的 src/planner/，通过命令行使用；现有桌面界面的推荐仍使用旧引擎。尚未接入实时推荐按钮、后台 Worker、自动保存决策记录或打包安装。

## 1. 运行

在原项目目录运行：

```powershell
pnpm test
pnpm build
pnpm planner demo --output work/planner-demo.json
pnpm planner coverage --output work/planner-coverage.json
pnpm planner recommend 输入.json --output 工作结果.json
```

沿用现有 pnpm、TypeScript、Vite 和 Vitest，没有新增依赖。命令行将模块临时编译到 work/planner-runtime/，不会调用游戏窗口、识别服务或实际存档。

recommend 输入为 JSON 对象，包含：

- state：完整 PlannerState，包括六个槽位、当前候选、常驻获得回合、剩余重抽次数。
- model：可选的模型配置覆盖。首次实验必须显式写 offerPool 和 poolLabel。
- search：可选的 simulations、policySamples、beamWidth、policyDepth、seed、maxTransitions。

演示输出的 input 字段可以直接保存为输入 JSON。没有输入时，demo 使用合成局面和五张真实药剂构成的限制牌池，不能把该期望当作完整游戏期望。默认完整底表池尚有未迁移卡牌，运行完整规划会返回 blocked，而不是偷偷排除未知牌。

CLI 的 recommend 返回 complete 才有推荐；blocked 和 budgetExceeded 退出码为 2。无效 JSON 或状态会报错，不自动修正。

## 2. 已实现

| 模块 | 文件 | 内容 |
|---|---|---|
| 状态和规则协议 | src/planner/types.ts | 固定六槽，怪物身份与槽位身份分离，常驻运行状态，动作、事件、规则算子 |
| 规则模拟 | src/planner/simulator.ts | 全部可选目标组合、随机目标采样、删除/添加/满槽/变异/两组融合、事件队列、13轮推进 |
| 经验转换 | src/planner/empirical.ts | 四级样本回退、显式先验、缺样本阻断、低置信度说明 |
| 搜索 | src/planner/search.ts | 蒙特卡洛终局评价、候选动作 Beam 筛选、未来策略 rollout、资源预算、器械顺序检查 |
| 识别和校准 | src/planner/observations.ts | 旧状态和 RecognitionSnapshot 适配、完整观测覆盖、DecisionRecord、JSONL 导出 |
| 真实卡规则 | src/planner/catalog.ts | 沿用原 ID，19 张药剂、7 张常驻，其他规则显式标为未实现 |
| 演示和入口 | src/planner/demo.ts、index.ts、scripts/run-planner.mjs | 可复现案例、JSON 输入输出、覆盖表导出 |

本轮牌效果完成后，FIFO 处理其产生的事件，再发送 roundEnd 并处理常驻连锁。事件保存发生时的前后怪物快照；作用到新增怪物的事件还检查 instanceId，避免误加到后来复用同一槽位的新怪。

本轮获得器械设 firstEligibleRound = acquiredRound + 1。第 11～13 轮只有推进动作，仍执行常驻；第 13 轮结算后状态为 round=14，终止搜索。重抽只扣一次资源并替换候选，不推进回合、不触发 roundEnd。

每次添加先检查容量。满槽不需要虚构新怪属性、不产生 addSucceeded，但产生 addFailedBoardFull，供专用规则监听。两组融合采用数量求和、单位活性求和，产生两次删除、一次成功添加和一次融合完成。

种群单独变化保留数值；稀有度变化查经验模型；首领稀有度封顶。中间生成结果必须是整数，不能用小数期望当作真实怪物。最终均值和分位数允许为小数。

## 3. 真实卡覆盖

已实现数值加成、条件效果和种群变异：

- 软脑膜溶液、益生霉溶液、石化脊髓溶液、消化酶溶液。
- 孪生激素-骨卫兵、麻药酊剂-异魔、生骨药粉、清疽油膏。

已实现生成流程，但必须提供对应 monsterId、种群、稀有度、数量、单位活性基础字典：

- 细肢药粉-蛊虫、脊髓溶液-觉醒者、细肢药粉-骨卫兵、脊髓溶液-异魔。
- 活性育卵激素、靶向异种激素。

已实现稀有度流程，但在实际发生稀有度变化时必须有经验样本或显式先验：

- 迷魂酊剂、至纯圣水、麻药酊剂-觉醒者、青胆汁溶液、诱变药粉。

已迁移常驻：

- 脏污刮骨刀、挛缩指爪、孽生肉芽、蠕动脊髓。
- 肿大脑垂体、生皮革拘束带、簇生虫卵（新增基础数据仍需输入）。

“已迁移”是程序具有对应算子，不代表所有细节已获现版本实测验证。底表中额外器械即使原程序未有稳定 ID，也不得猜测识别匹配；未知 ID 会阻断。

未迁移牌保留名称/ID和失败原因。例如人蛹重复次数、蜕生皮的一组/混合稀有度融合、药剂箱同轮展开、复制类和特殊连续药剂，都不能当作空效果参与比较。

## 4. 搜索解释

当前每个合法根动作均独立评估，不能因为初始数值较低就排除。每个动作采样模拟至第13轮，按最终总活性均值排名。

后续策略先用多次采样评价各动作的下一步活性，保留 beamWidth 个候选动作，再用终局 rollout 比较。policyDepth=0 使用采样贪心策略；1 或 2 增加后续规划，计算量也提高。这里的 Beam 是动作筛选，而不是只保留幸运随机结果。按概率平均所有被采样的结果，避免把高收益随机分支当作必然发生。

策略内部采样和实际模拟轨迹使用独立随机流。策略只拿到当时的状态和可见候选，不能知道轨迹下一次会发什么牌。所有根动作使用可复现种子；最终报告包含实际样本数和状态转移数。

输出包含 mean、median、p10、p90、standardError、simulations、modelConfidence。standardError 只描述有限采样的误差，不包括规则错误、未知牌池、未知真实概率、经验迁移偏差或近似策略的次优性。conditional 表示在所声明模型和策略下有效，绝不等于真实游戏高置信度。

搜索不是全局最优证明。动作 Beam 可能剪掉短期弱、长期强的路线；完整牌池或大量目标下应增加宽度与样本，或后续改为 MCTS。预算耗尽或任一候选遇到未建模效果时，不给整体推荐；已完成的估值只供诊断。

顺序敏感性检查包括获得顺序、反向顺序和逐回合随机排列抽样。不混合为一个假装已知的概率分布；保留三个情景各自的报告。stable 仅表示此次有限检查推荐一致。

## 5. 明确假设与尚未支持部分

1. 跨轮发牌独立且合法卡等概率；同轮默认不放回，可改为放回。原工作流未规定同轮重复语义，须实测。
2. 生成采用种群→稀有度→具体怪物分层等概率。字典覆盖不完整时，分布仅对字典提供的结果有效。未指定生成稀有度时，不擅自沿用旧版普通怪默认值。
3. 经验变异的前三层用样本“数值差值”迁移到当前成长怪物；第四层从目标稀有度分布采样并替换数值。差值是否比重置/比例模型准确，需独立测试。无数据则阻断；不把未知视为零收益。
4. 常驻条件在事件处理时读取局面。跨阈值、器械相互触发、同类变异是否仍算一次事件仍需确认。当前属性完全未变化不发 mutationCompleted。
5. 两组融合的通用算子已测试；混合稀有度必须显式提供结果稀有度，不能猜。真实蜕生皮的一组融合仍未迁移。
6. 阶段字段、卡牌阶段资格和 nextStage 已支持；真实特殊药剂链和药剂箱没有完整配置。
7. 培养途中额外奖励常驻的出现时点未模拟；初始常驻获得回合必须由调用方提供，不能从现有列表倒推。
8. 识别适配是严格接口，要求完整六槽、总活性、候选、常驻、重抽信息。旧 OCR 在部分画面不提供这些字段时会拒绝；不是已完成的桌面实战接入。
9. 未新增神经网络训练、GPU任务、在线自动学习或自动游戏操作。

## 6. 校准和训练数据

fromLegacyState 需要显式元数据；旧 state 里的 monster.id 是槽位，不能误当成怪物物种 ID。

fromRecognitionSnapshot 要求置信度至少0.9、六槽唯一完整、组总数/全局总数一致、回合匹配、常驻集合可对齐。新器械必须另行记录获得回合。不完整字段不会从预测中拼补成“已识别真值”。

reconcileObservation 验证通过后返回新真实状态和 DecisionRecord；未通过则保留操作前实态并加 review，停止继续决策。日期不递增的旧帧/重复帧被拒绝。不会将预测自动写入真实存档。

DecisionRecord 包含对局 ID、记录 ID、时间、规则版本、所选卡和目标、前态、预测、后态、误差及搜索报告。serializeRecords 输出 JSONL 并拒绝重复 ID。保存到文件由调用方执行。

trainingEligible 只代表该记录可用于后续数据审查，不代表已经归因清楚。稀有度样本另外要求 reviewed 和 isolatedMutation：整轮前后差值混合了卡牌显式加成、常驻和多个事件，不应直接当成稀有度转换收益。

## 7. 验证结果

新增53项测试，涵盖事件计数、固定槽位、满槽专用事件、器械延迟、完整13轮规划、手术阶段、随机分支期望、未来信息隔离、重抽、条件牌池、预算保护、经验回退、校准拒绝和JSONL。

三段既有截图轨迹在新模块重放，准确匹配：

| 对局记录 | 操作 | 结算总活性 |
|---|---|---:|
| 第5轮→第6轮 | 消化酶 + 既有常驻 | 68,328 |
| 第8轮→第9轮 | 消化酶 + 既有常驻 | 331,980 |
| 第10轮→第11轮 | 生骨药粉 + 既有常驻 | 463,722 |

这些是保存轨迹的离线验证，不是本轮新采集的游戏画面。最终全量测试与构建结果见 QA.md 最新记录。

演示种子20260907、每动作32条终局轨迹：推荐软脑膜溶液→槽1，终局均值112,306，中位数113,860，p10=103,942，p90=118,600。演示中共41,578次状态转移，本机一次运行约1.7秒；性能不是全牌池承诺。
