# 常规药剂规则校准：2026-09-08

后续更新：特殊药剂已追加8张共享分支，当前419测试、33药剂算子；胆汁空右/最右目标仍+31已确认并修复。下方为前一阶段原始记录，最新以 SPECIAL_POTION_CALIBRATION.md 为准。

来源：用户在当前对话逐张确认；不是网络推测。特殊药剂明确留到下一段对话，本轮未推进。

## 已确认并接入的 7 张

| 药剂 | 确认规则 | 当前实现 |
|---|---|---|
| 孪生激素-蛊虫 | 同名意味着相同种群、相同稀有度；添加者使用基础值，不复制成长值 | 所选组变异为蛊虫，新增组读取该稀有度基础数量/单体活性；原组沿用原有数值规则 |
| 卵壳药粉 | 同名新蛊虫使用基础值，并在基础值上加数量 | 随机原目标 +25；若是蛊虫，新增同种群同稀有度基础组并 +25，不能复制已增长的原目标 |
| 黏稠胆汁溶液 | 只改变右侧怪物的种群和稀有度，其他属性不复制 | 保留右侧原数量/活性，然后按卡面双方 +31 活性；不是移除再添加 |
| 蜕生皮溶液 | 不允许只选一组；两组稀有度相同/不同都随机决定结果稀有度；数量加数量、活性加活性 | 必须两目标、结果蛊虫；独立随机稀有度，不保留第一目标稀有度，不额外叠加出生/升阶属性 |
| 异种激素 | 新怪普通到首领均可能出现；按稀有度使用基础数量/活性；先移除再添加 | 移除全部已选1–2组，再作4次独立随机种群/稀有度添加，单体活性在基础值上 +5；容量失败仍有事件 |
| 诱虫剂 | 溢出增益包含本张刚添加的蛊虫，每次溢出分别触发 | 逐次添加4组；每次失败给当前蛊虫 +10 单体活性、+15 数量；接入成功添加/满槽失败常驻 |
| 活性育卵激素 | 新生稀有度随机、基础数量/活性由稀有度决定；额外两组分别随机 | 先添加1组；50%额外添加2组，每组单独掷稀有度，不共用一次结果 |

共享声明：`src/planner/confirmedPotions.ts`。模拟执行：`src/planner/simulator.ts`。桌面桥接：`src/engine/confirmedPotions.ts`、`sharedPassives.ts`。

## 数值仍需补充，不能视为全数据完成

四个稀有度的真实基础数量、单体活性还没有提供。新增 `persistentModel.rarityBases`，独立于变异经验样本和先验；结构为：

```ts
rarityBases: Partial<Record<'common' | 'magic' | 'rare' | 'boss', {
  quantity: number;      // 正整数基础数量
  unitActivity: number;  // 正整数基础单体活性
}>>
```

桌面“设置 → 常驻结算参数 → 导入常驻模型数据 JSON”，顶层字段名为 `rarityBases`，界面显示已填 0–4 档。非法数据拒绝；导入后重算并随局面保存。没有更改 OCR 名称字典。旧 `newbornSwarm` 单一属性保留兼容，但不再供这7张药剂使用。

若显式 `monsters` 基础字典中同稀有度的数值完全一致，可作为该档基础值；存在冲突则要求独立填写 `rarityBases`。不能把 `rarityPriors` 的变异数值当成出生值，也不能从场上成长怪物推回基础值。随机出生需要所有合法档位数据，缺一档会阻断，不会悄悄缩小随机范围。满槽失败不依赖基础值，仍能触发溢出效果。

## 明确保留的边界

- 随机稀有度、种群出现权重未获确认。模型暂用四档/四种群等概率情景，不称实测概率；每个新组独立抽取。融合结果同样暂按四档支持集处理。
- 黏稠胆汁右侧为空/最右槽时，原目标 +31 是否仍生效未确认；该目标分支报待确认，不擅自设为无效果或仅原目标加成。
- 同类种群/稀有度不变时是否算一次变异仍需实测；本轮未扩展此事件语义。
- 卡牌本体完成后 FIFO 常驻仍是默认；诱虫剂按每次添加/溢出处理并结算常驻，再进行下一次。与蠕动脊髓等相互影响的精确先后仍需实战核验，未将用户未描述的连锁顺序标为确认。
- 黏稠胆汁是身份复制而非“升级”，按用户说明匹配右侧稀有度；融合产生新怪，随机结果不受输入首领限制。其他升阶算子的首领上限保护未修改。
- 桌面七张均为预览，实际局面保持原样；游戏使用后 F8 读取完整结果。只给旧 `observedAddedGroupMutationIdsByCardId` 不能确定新增融合稀有度，不当成完整观测写回。
- 有界枚举超预算后32样本，不是精确概率分布/真实保底；抽样结果不启用“确定启动”优先。缺数据/规则时停止完整推荐、自动应用和洗牌概率估计。
- 示例用常驻尚未接入这7张共享效果，会明确标注不支持；真实24张共享常驻支持。特殊药剂不在本轮范围。

## 校验与交接

- 基线365项；新增23项逐卡/基础值测试，共388项通过。旧测试改为检查新语义：不复制成长值、融合两目标随机稀有度、固定单蛊虫配置不再生效、未知数据显式保留。
- 原第7回合融合实战记录仍在完整枚举合法分支中；即时和回合末均匹配，既保留实战证据也不把一次结果推广成确定规则。
- TypeScript/Vite生产构建通过；Planner覆盖25张药剂算子/24张常驻（育卵为原有算子的替换，所以19+6=25）。CLI demo仍完成，41578转移，无issues。
- Playwright/Edge，独立浏览器上下文，识别接口模拟：1440×1000与390×844。缺数据不显示推荐标记→错误JSON拒绝→4档合成测试值导入→恢复计算→融合自动建议两目标→刷新保留；真实局面不被预览修改。控制台无错误、无框架遮罩、无横向溢出。Browser plugin not available，未安装额外依赖。
- 脚本/截图位于外层 `work/potions-qa.cjs`、`potions-desktop.png`、`potions-mobile.png`，不在Git项目中。测试数值均为合成，不是真实基础数据。
- 复杂三候选（异种/育卵/诱虫）+三常驻浏览器抽测首次约118ms，缓存后10次均值约0.21ms；仅该情景，不是所有组合延时承诺。
- 本轮未提交/推送/打包NSIS/安装，没有修改真实字典、存档或识别服务。下一步补四档真实基础数值与上述边界；特殊药剂等下一段用户说明。
