# 客户端自动记录 V1

2026-09-08：已通过保数据升级安装到D:/渴瘾决策器，12条名字及存档哈希未变，待实际游戏验收。旧的离线学习原型继续可用；没有替换推荐算法。

## 使用

1. 更新后打开客户端，默认“自动记录开启”。首次按 F8 开始记录，不需要另开训练或常驻截图。
2. 游戏中点牌、点确认，下次 F8 同步。选牌监听提供暂选、确认、换面等证据；未捕获到的选择保持未知。软件里的实际选择确认也会记录。模拟执行按钮、修改示例数值不会冒充真实操作后画面。
3. 顶部及浮窗的“牌局记录”可以看本局/历史、分类、展开六罐与候选，以及导出。确认轮次回退自动分局，但同为第1回合重开无法仅凭画面确定，请点主界面“新牌局”。
4. 不想采集时“暂停记录”，恢复后保留缺口提示。暂停只停止归档，不停止F8识别或修改推荐。
5. “结束本局记录”只是停止该片段，不是确认游戏终局。下次新 F8 自动开始另一片段。重启客户端也建立新片段；历史仍可查看，不会把服务缓存截图当新一次操作。中途重启/导入会产生不完整片段，当前不自动归并。

导入截图总是独立片段，不混入当前实战；请勿把独立截图当成一整场。未识别成功的六槽保留原始结果，不能用之前画面补齐；可靠空罐可记录为空，不要求每次手工确认。

## 保存与备份

原生路径：`%LOCALAPPDATA%/VoraxDecisionAssistant/user-data/recorded-runs/<runId>/<eventId>.json`。

- 与 `monster-dictionary.json`、`workspace.json` 分离，不依赖安装位置、WebView缓存或版本号。升级不清理；换电脑/重装系统仍须备份整个目录。
- 每次事件独立不可变：先同步临时文件，再原子发布；同ID重试幂等，冲突拒绝。没有“最多100条后自动丢弃”限制。每次读取按sequence重建，缺失/损坏不会悄悄修补。
- 保存队列失败会显示红色提示，保留待写事件，可“重试保存”或“导出未保存备份”。有待保存或错误时不要关闭客户端；没有写入磁盘的内存事件在强制结束进程后无法保证恢复。暂停不清除队列。
- 单事件上限1MB；单局界面预览32MB。超限明确报错，不删除已有记录，可直接备份磁盘目录。
- 网页预览仅使用浏览器localStorage，不具有原生独立存储保障；正式使用请安装本次新版。老客户端不支持新命令会报保存失败，不假报保存成功。
- 本版保存结构化原始识别结果与时间/来源，不保存PNG位图、不录屏、不上传。模型与导出同样可能包含个人对局，不默认推送GitHub。`*.journal.json`、`*.runs.jsonl`、`recording-recovery.json`已忽略。

## 记录内容与学习边界

每局包含start/capture/choice/gap/end事件；截图有服务capturedAt和客户端recordedAt、原始六槽、卡名、置信度、显示总活性，以及独立校验的RunFrame（无法完整结构化时为null）。种群/稀有度可由本次高可信名称命中字典解析，不能借用上次怪物。当前软件勾选的常驻另记为软件知识；关联确认的常驻选择可以记录获得回合，初始来源未知的年龄仍是null。

选择状态更新追加为历史，按记录ID去重统计选择数。自动绑定需回合、阶段、牌位和完整名称匹配，初次服务响应中的历史选择不套到新局。重复轮询不会重复落盘；同候选且无新选择证据时复用offerId。不知道是否“重抽恰好同牌”时不臆造一次新发牌，不能直接用这些频次断言真实抽牌概率。

导出格式：

- **原始记录 `.journal.json`**：完整事件、识别失败、所有选择状态变化、缺口；这是优先保留的证据。
- **学习档案 `.runs.jsonl`**：兼容 `pnpm learning audit 文件.runs.jsonl`；只转换可结构化前态，不完整原始观察仍在原始记录中。每个自动步骤 `singleActionConfirmed=false`，随机落点/目标未验证，终局 `incomplete`。无法可靠确认“期间仅操作一次”，因此不会直接进入可靠监督训练。

界面的分类是回合、阶段、种群、稀有度、常驻组合、零蛊虫人蛹等观察标签，不是胜率或最优操作标签。结束记录/13回合画面不是“已完成13轮全部结算”的证据，因此绝不拿最后一次截图作为最终收益训练。

下一阶段：真实运行验收与完整率报告，按局归并与逐步校订、实际终局确认，再积累足够真实训练/验证/测试局。当前属于采集基础设施，不是已自动学会打牌。
