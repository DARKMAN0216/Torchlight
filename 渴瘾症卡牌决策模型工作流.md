# 《火炬之光：无限》渴瘾症卡牌决策模型工作流

## 1. 目标

在渴瘾症玩法的怪物培养阶段，读取当前怪物组、候选卡牌、常驻器械与回合信息，模拟每个合法选择的未来结果，推荐最终活性期望最高的卡牌及目标。

模型以规则模拟和概率搜索为核心，不使用固定的卡牌强度评分。每次实际操作后重新读取游戏画面，以真实结果校准模型状态。

最终活性定义为：

```text
最终活性 = Σ（每组怪物数量 × 每组怪物单位活性）
```

数值均为正整数，不存在额外稀有度倍率或小数取整。

## 2. 已确认规则

### 2.1 回合

- 整局共 13 轮。
- 第 1～10 轮为怪物培养和卡牌选择阶段。
- 第 11～13 轮选择手术方案；手术方案不参与怪物培养决策。
- 已获得的常驻器械在第 11～13 轮仍会继续生效。
- 本轮获得的常驻器械不会在本轮生效，从下一轮开始生效。

### 2.2 怪物槽位

- 最多存在 6 组怪物，并保持固定槽位映射。
- 添加怪物时按空槽顺序补入。
- 槽位不足时，超出部分不会生成，也不会触发普通的“成功添加怪物”效果。
- 明确写有“槽位已满时……”的卡牌或常驻效果，需要监听生成失败或满槽事件，单独结算。

### 2.3 删除、添加、融合与变异

- “删除一组并生成两组”产生 1 次删除事件和最多 2 次成功添加事件。
- 每次生成均按顺序执行；没有空槽时，该次生成失败。
- 融合两组怪物产生 2 次删除、1 次添加和1次融合完成事件。
- 变异保留原槽位。
- 只改变种群的变异不改变数量和单位活性。
- 改变稀有度的变异可能改变数量和单位活性，具体变化规则暂未确认。

### 2.4 结算顺序

当前已确认的主顺序为：

```text
读取本轮局面与候选牌
→ 选择卡牌和目标
→ 执行卡牌效果
→ 处理卡牌引起的删除、生成、融合、变异事件
→ 结算此前获得且本轮符合条件的常驻器械
→ 读取新画面并校准真实状态
→ 进入下一轮
```

多张常驻器械之间的内部结算顺序尚未确认，第一版暂按获得顺序执行，并进行顺序敏感性检查。

## 3. 默认概率假设

### 3.1 发牌

- 每轮从当前符合条件的牌集合中独立抽取。
- 普通情况下，所有合法卡牌按相同概率处理。
- 官方存在低概率卡牌，但第一版忽略其权重差异。
- 条件卡牌先根据轮数、种群、特殊药剂阶段等条件筛选，再在合法集合中等概率抽取。
- 特殊药剂按其阶段规则单独维护。

### 3.2 随机目标和随机生成

- 所有合法目标默认等概率。
- 卡牌明确指定种群或稀有度时，只在符合限制的怪物集合中随机。
- 没有卡牌限制时，种群、稀有度和具体怪物均按合法结果等概率处理。
- 规划时使用随机分支或蒙特卡洛抽样。
- 实际选择后，以下一帧识别到的真实怪物、数量和活性覆盖预测值。

## 4. 核心状态

```ts
interface GameState {
  round: number;
  totalRounds: 13;
  decisionRounds: 10;
  maxGroups: 6;

  monsters: MonsterGroupState[];
  persistentEffects: PersistentRuntimeState[];
  offeredCardIds: string[];
  redrawsRemaining: number;
  specialPotionStage: number;

  recognitionReview: string[];
}

interface PersistentRuntimeState {
  cardId: string;
  acquiredRound: number;
  firstEligibleRound: number;
  triggerCount: number;
  enabled: boolean;
}
```

获得常驻器械时：

```ts
firstEligibleRound = acquiredRound + 1;
```

## 5. 动作定义

一个完整动作必须包含卡牌和目标，不能只比较卡牌 ID。

```ts
type GameAction =
  | {
      type: 'playCard';
      cardId: string;
      selectedSlotIds: string[];
    }
  | {
      type: 'redraw';
    }
  | {
      type: 'advanceSurgeryPlanRound';
    };
```

`GetLegalActions(state)` 负责枚举所有合法卡牌、目标组合和重抽操作。第 11～13 轮只产生 `advanceSurgeryPlanRound`，但仍结算常驻器械。

## 6. 事件系统

```ts
type GameEvent =
  | 'cardPlayed'
  | 'removeSucceeded'
  | 'addAttempted'
  | 'addSucceeded'
  | 'addFailedBoardFull'
  | 'mutationCompleted'
  | 'fusionCompleted'
  | 'roundEnd';
```

### 删除一组并生成两组

```text
删除选中槽位
→ removeSucceeded
→ 尝试生成第1组
→ addSucceeded 或 addFailedBoardFull
→ 尝试生成第2组
→ addSucceeded 或 addFailedBoardFull
```

### 融合

```text
删除目标A → removeSucceeded
删除目标B → removeSucceeded
生成融合结果 → addSucceeded
触发 fusionCompleted
```

### 变异

- 不转化为删除和添加事件。
- 只触发 `mutationCompleted`。
- 种群单独变化时保留数量和单位活性。
- 稀有度变化时使用经验转换模型，并在操作后用真实画面校准。

## 7. 稀有度变异的临时模型

记录每次操作前后的转换样本：

```ts
interface RarityTransitionSample {
  cardId: string;
  beforeMonsterId: string;
  afterMonsterId: string;
  fromRarity: RarityId;
  toRarity: RarityId;
  beforeQuantity: number;
  afterQuantity: number;
  beforeUnitActivity: number;
  afterUnitActivity: number;
}
```

规划时依次使用：

```text
相同卡牌、相同前后怪物的历史样本
→ 相同卡牌、相同稀有度变化的样本
→ 所有卡牌中相同稀有度变化的样本
→ 对应稀有度怪物的总体平均值
```

如果完全没有样本，应把本次估值标记为低置信度，不将未知效果当作零收益。

## 8. 单轮实时工作流

```text
游戏画面
→ 获取软件读取轮数、6个槽位、怪物ID、种群、稀有度、数量、单位活性、候选牌、常驻器械
→ 标准化为 GameState
→ 枚举全部合法动作
→ 为每个动作模拟当前卡牌效果
→ 模拟随机目标、随机生成和稀有度变化
→ 模拟后续发牌与未来选择
→ 模拟至第13轮结束
→ 比较最终活性分布
→ 推荐卡牌与目标
→ 玩家实际操作
→ 再次读取画面
→ 用真实结果覆盖预测状态
→ 保存 DecisionRecord
→ 下一轮重新搜索
```

模型不要求一次性准确预测整局。每轮操作后都以识别到的真实状态重新开始搜索，避免随机误差持续累积。

## 9. 决策算法

第一版推荐使用“蒙特卡洛模拟 + Beam Search”：

1. 对当前每个合法动作生成操作后状态。
2. 对随机结果和未来发牌进行多次采样。
3. 每个深度只保留预计价值最高的若干状态。
4. 一直模拟到第 13 轮结束。
5. 按最终活性的平均值选择动作。

每个动作至少输出：

```text
预计最终活性均值
最终活性中位数
10%分位保底值
90%分位高收益值
模拟次数
模型置信度
```

默认目标为最大化最终活性期望：

```text
BestAction = argmax E[FinalActivity | State, Action]
```

## 10. 常驻顺序不确定性的处理

第一版默认按照常驻器械获得顺序结算。同时检查其他可能顺序：

```text
获得顺序
相反顺序
随机顺序抽样
```

如果不同顺序下推荐相同，正常输出。如果推荐不同，输出顺序敏感警告及各情景的推荐结果。

重点检查以下非交换效果：

- 当前最高或最低活性目标选择。
- 加法和乘法混合。
- 添加、删除、融合或变异。
- 满槽触发。
- 改变后续器械触发条件。

## 11. 操作后校准

操作完成后，通过固定槽位映射比较前后状态：

```text
预测状态
实际识别状态
→ 确认随机目标和生成结果
→ 更新怪物基础数据统计
→ 更新稀有度变异样本
→ 记录预测误差
→ 保存本轮决策记录
```

如果识别置信度不足，则停止自动决策或提示人工确认，不能把低置信度结果直接写入训练数据。

## 12. 默认规则配置

```ts
const defaultRules = {
  totalRounds: 13,
  decisionRounds: 10,
  maxGroups: 6,

  offerDistribution: 'uniformAmongEligibleCards',
  cardEffectsBeforePersistentEffects: true,
  newPersistentEffectDelayRounds: 1,
  persistentOrder: 'acquisitionOrder',

  addPosition: 'firstEmptySlot',
  failedAdditionProducesGroup: false,

  raceOnlyMutationPreservesStats: true,
  rarityMutationStats: 'empiricalDistribution',

  fusionEvents: {
    removals: 2,
    additions: 1,
    fusionCompleted: 1
  },

  deleteAndGenerateTwoEvents: {
    removals: 1,
    additionAttempts: 2
  }
};
```

## 13. 待验证项

以下问题暂不阻碍第一版实现，但需要通过后续真实对局校准：

1. 多张常驻器械之间的准确结算顺序。
2. 同一种怪物多次随机生成时，初始数量和单位活性是否固定。
3. 稀有度提升或降低时，数量和单位活性的准确变化规则。
4. 特殊低概率牌的真实权重。
5. 条件卡牌的完整合法性条件和特殊牌池限制。

## 14. 推荐实现顺序

```text
第一步：完善稳定ID、怪物字典和卡牌规则
第二步：实现 GameState 与合法动作生成
第三步：实现确定性卡牌效果和事件队列
第四步：实现随机生成与稀有度经验模型
第五步：实现13轮状态推进与常驻器械结算
第六步：使用真实对局逐轮验证状态转换
第七步：实现蒙特卡洛模拟和 Beam Search
第八步：接入现有信息获取软件
第九步：记录实战结果并持续修正经验分布
```

在规则模拟器通过真实对局验证之前，不建议训练神经网络。后续如果搜索速度不足，可使用搜索结果训练价值模型，为 Beam Search 或 MCTS 提供快速局面估值。
